# Протокол проверки — 2026-09-26

Среда: Windows x64, Temurin JDK 25.0.4.1+1, Gradle 9.2.1,
Minecraft 26.2, NeoForge 26.2.0.81, ModDevGradle 2.0.146.

## Проверено

- `build`: Java-код компилируется, мод собирается в JAR.
- JUnit: **172 теста**, 0 ошибок, 0 пропусков.
- Python: **307 проверок**; локально выполнены тремя непересекающимися пакетами
  102/102/103. Сверка с полным discovery подтвердила отсутствие пропусков и дублей;
  ошибок и пропусков нет.
- Выделенный Minecraft GameTest server: **1523 теста Techguns + 1 встроенный**,
  все 1524 прошли; условная химия/молоко NeoForge/жидкости камеры/электрум/бур — **ещё 15**,
  генерация руд, структур, добыча и выбор локаций — **ещё 28**.
  Последний отдельный прогон сборки/условных тестов завершился 2026-09-26 20:52 Europe/Zurich;
  всего **1567 GameTests**. Текущий срез: камуфляжные сети и навесы.
  Объединённый процесс завершился с кодом 1 без сообщения об ошибке при запуске
  условного сервера, после успешных 1524 + 28 тестов. Оставшиеся 15 и сборка
  повторены отдельной командой, завершившейся `BUILD SUCCESSFUL` с кодом 0.
  Minecraft-клиент не использовался.
- Linux CI [#57](https://github.com/PozziTiv4ik/Techguns2/actions/runs/36264230110)
  завершился успешно 2026-09-26 в 21:02 Europe/Zurich для source commit
  `b570fca304dbf7ae3dc0f0e98be3b357db947824`: все 13 шагов, 307 Python
  единым запуском за 211,549 секунды, сборка/ядро и все 1567 GameTests.
  Три серверных набора выполнялись раздельными командами и завершились штатно.
- Все **172 рецепта верстака, 19 обычной печи, четыре Ammo Press, 19 Metal Press, четыре Blast Furnace, 31 Chemical Laboratory, семь Reaction Chamber, шесть Fabricator, два Charging Station и 56 Grinder** загружаются через Minecraft RecipeManager;
  в последнем журнале нет ошибок разбора данных.
- По прямой просьбе пользователя 2026-09-09 запущен клиент предыдущего среза
  `9746170`: журнал подтвердил вход Dev в мир в 12:21 Europe/Zurich. Computer Use,
  мышь и клавиатура не использовались. Серверные проверки выполняются без клиента.
  По следующей прямой просьбе пользователя запущен срез от 2026-09-09: вход в мир подтверждён
  в 16:11, штатный выход — в 16:18. Найденные ошибки ресурсов и исправления описаны ниже;
  полная визуальная приёмка остаётся открытой.
- Каталог исходников: 761 Java-файл, 103043 строки, 2133 ресурса, 44 записи оружия.
- Дерево `legacy/1.12.2/src` побайтово совпадает с Git-деревом `src` исходного
  репозитория: оба имеют ID `270b27b1cc52019733bb79facda68ce29aa038be`.
- Генератор преобразует **651 деталь 22 моделей оружия, включая заряженную ракету, 71 деталь двух прессов, 57 частей лаборатории, 38 частей Fabricator, семь элементов домны и три 11-частных ракетных боеприпаса с отдельными моделями летящих ракет**, переносит исходный OBJ камеры на **334 треугольника** и OBJ зарядной станции на **22 части / 132 четырёхугольника**; формирует **2093 файла**, включая 14 частей SuperMutantBasic и 20 кубоидов/семь костей CyberDemon:
  параметры, Java-каталоги, cuboid/OBJ, материалы, ресурсы, теги и рецепты.
- Ресурсный граф: **269 item definitions (262 предмета, три модели летящей ракеты, вал Grinder и три секции бура), 557 ссылок на текстуры моделей и 72 звуковых события**;
  дополнительно проверены четыре анимированных спрайта жидкостей с `.mcmeta`
  и две исходные текстуры луча. Отдельно проверены 23 equipment definitions, 40 исходных слоёв T1 Combat/T1 Miner/T1 Scout/T2/Hazmat
  и три исходных PNG берета, скопированные в слои humanoid / humanoid_baby.

GameTests проверяют:

- Для каждого из 21 образца: реестр, перезарядку, сериализацию магазина, выстрел,
  число снарядов, исходные параметры пули и блокировку повторных запросов.
- Сохранение боезапаса; 45 тиков перезарядки револьвера; отмену при смене предмета,
  включая другой экземпляр того же типа; повторную проверку наличия патронов.
  Смена оружия не позволяет обойти общий cooldown. Недопустимые действия отклоняются.
- Попадание в сущность, защиту каменной стеной, время жизни, гравитацию ручной пушки,
  запись/чтение пули. Все восемь дробин наносят урон в одном тике.
- Возврат пустого магазина и целых остатков; загрузку дроби из нескольких стеков.
- Исходную формулу брони: при 10 единицах защиты и пробитии 0,5 урон винтовки —
  10,88; toughness 2 снижает его до 9,6.
- Восемь сценариев прицела: поддержка оптики, отмена перезарядкой/сменой стека/смертью,
  центрированная точка вылета, отсутствие доверия к поддельному флагу HUD,
  исключение временных флагов из NBT и однобайтовый сетевой codec.
- Загрузку всех 131 рецепта верстака; изготовление деталей, ручной пушки, револьвера и патронов;
  заряженный/пустой Thompson; пять типов магазинов с последующей R-перезарядкой;
  упаковку/распаковку патронов и металлов без изменения количества.
- Позолоту при 0, 3 и 6 патронах с сохранением имени, независимостью входного стека
  и исключением флагов действий; сетевую сериализацию особого рецепта. Выбор
  предпочтительного тега и fallback при отсутствии материала проходит codec round trip.

Рецептные тесты используют реальные `matches`/`assemble` и серверную логику оружия.
Они не проверяют управление экраном верстака. Добыча и плавка руд проверяются
отдельными сценариями ниже. Shift-click отдельно проверяется для меню Ammo Press.

Дополнительные 18 GameTests Ammo Press проверяют:

- Все четыре реальных производственных цикла: 100 тиков, 500 FE, выход 12/16/8/4.
- Отсутствие энергии, остановку на 99 тиках без последних 5 FE и последующее завершение.
- Режимы редстоуна и паузу без потери прогресса; блокировку выхода до/после начала работы.
- Пакетную обработку: семь улучшений, ограничение выходом 64, две партии 60 + 36
  пистолетных пачек при расходе 4000 FE за восемь исходных наборов.
- Сериализацию и замену BlockEntity в середине операции, сохранение прогресса,
  энергии и следующего плана; текущая партия остаётся прежней.
- Разрушение: выпадает сам блок и ровно 1/2/1 зарезервированного сырья, без будущего результата.
- Транзакции предметов и энергии, включая откат, ёмкость 20 000 FE и ограничения сторон/слотов.
- Владельца, открытое меню, дальность кнопок, неизвестные действия и shift-click.
- Сетевой codec производственного рецепта и исходный режим без потребления энергии.
- Настоящие воронки над и под станком: загрузка сырья и извлечение ровно 12 пачек.
- Исходный крафт проволоки, двигателя, пресса и пакетного улучшения с максимумом семь предметов в стеке.
- Чтение поля плана из сохранения первого порта после перехода на общую реализацию прессов.

Проверки прессовых циклов, воронок и восстановления используют обычные тики
BlockEntity в настоящем ServerLevel. Меню проверяется серверными вызовами, а не мышью.
Режим без питания проверяется синхронными вызовами серверного тика с восстановлением
значения конфигурации после теста. Обычная сборка по умолчанию требует FE.

28 GameTests Metal Press проверяют:

- Все 19 исходных рецептов, каждый в двух расположенных в мире машинах с разным
  порядком входов. Проверяются выход/количество, 100 тиков и 2000 FE на обычную партию.
- Авторазделение с обеих сторон: нечётный стек из пяти слитков, компоненты стека,
  четыре готовые пластины и один оставшийся слиток после двух циклов.
- Отказ от авторазделения при отсутствии рецепта одинаковых входов и проверку второго материала.
- Три улучшения и четыре пары сырья: фактические 320 FE/тик и 32 000 FE за цикл,
  включая перезагрузку BlockEntity в процессе и отображаемое меню требование энергии.
- Разборку работающего пресса: исходные предметы возвращаются с компонентами;
  будущий результат не выдаётся второй раз.
- Два настоящих пресса и воронку между ними: железо → пластины → механическая деталь;
  остаток пластины сохраняется, обе стадии оплачены энергией.
- Меню, shift-click, ограничения пары/выхода, корректные индексы четырёх слотов,
  транзакционный откат, codec рецепта с правилом перестановки и оба рецепта станка.

Переход на общую основу сначала выявил ошибку порядка инициализации: обёртка
транзакционного инвентаря запоминала размер до создания слотов. Обёртка теперь
создаётся после инвентаря. Повторно прошли тесты обоих прессов, включая работу
с последним слотом, извлечение результата и откат транзакции.

15 GameTests Blast Furnace проверяют:

- Четыре исходных рецепта: количества, фиксированный порядок входов, длительность
  800/800/200/100 тиков и точный расход энергии.
- Отказ рецепта при трёх железных слитках вместо четырёх и перестановке входов;
  сохранение количеств и времени при сетевой сериализации рецепта.
- Пакетную бронзу из 12 меди и четырёх олова: 16 бронзы и 16 000 FE за цикл.
- Загрузку длинной партии стали после 200 тиков с сохранением энергии свыше 20 000 FE;
  итог — восемь стали за 32 000 FE с учётом квадратичного множителя.
- Передачу 40 000 FE и таймеров 70 000/72 000 через настоящий codec
  `ClientboundContainerSetDataPacket` с восстановлением значений в клиентском меню.
  Общая обёртка разделяет логические числа на два 16-битных поля стандартного пакета.
- Размещение слотов, shift-click, отсутствие посторонних действий, точный возврат
  зарезервированных материалов и блока при разборке.
- Домну, воронку и Metal Press в одном мире: четыре железных слитка превращаются
  в четыре стальные пластины после двух стадий обработки.
- Ёмкость 40 000 FE, откат транзакций, ограничения входов, режим без питания,
  остановку редстоуном и исходный рецепт самой домны.

Конвертер явно различает обычный и древесный уголь из metadata 1.12.2;
неизвестные metadata не переводятся молча. Семь элементов модели домны и их UV
сравниваются с оригинальным JSON. Клиентские частицы, звуки и GUI пока проверены
исходниками/компиляцией; ручная приёмка остаётся открытой.

Проверка журнала выявила, что старое поле `type` для особого ингредиента не подходит
NeoForge 26.2: два рецепта SCAR пропускались, несмотря на остальные зелёные тесты.
Исправлено на `neoforge:ingredient_type`; добавлен отдельный тест присутствия каждого
сгенерированного рецепта в настоящем RecipeManager. Повторный запуск прошёл без этих ошибок.

Восемь GameTests руд проверяют:

- Пять исходных значений hardness, требования к восьми ванильным инструментам,
  включая новую медную кирку; взрывоустойчивость, свет и codec состояния блока.
- Один блок добычи с обычной, зачарованной на Удачу III и Silk Touch киркой.
- Различие титановой руды/обработанного титана, общие теги и реальную загрузку
  configured/placed feature; ровно одно подключение к каждому подходящему биому.
- Все пять флагов по отдельности и вместе, исходный порядок генерации, один размер
  на тип/чанк и последовательность выборов X/Y/Z. Отключённые руды не расходуют RNG.
- Настоящее размещение каждого вида в камне на отрицательных координатах чанков,
  независимость абсолютных высот от Y начала feature; отказ от земли/глубинного сланца.
- Запрет генерации в Nether и End до выбора случайных чисел.

23 GameTests обычной печи проверяют 19 циклов по 200 тиков, исходный опыт,
многопредметные выходы, блокировку при недостатке места, восстановление операции,
отсутствие прямой плавки сырых титана/урана и передачу свинца через воронку в
Ammo Press. Итог производственной цепочки — 12 пачек за 500 FE пресса.

Отдельная задача `runOreWorldTestServer` загружает тестовый datapack с обычным
шумовым ландшафтом и проверяет 16 новых чанков на seed 0. Все пять руд найдены
после настоящей генерации чанков; ручного вызова feature в этом тесте нет.
Пакет теста не попадает в JAR: проверено содержимое собранного архива.
Проверка нескольких seed и клиентская приёмка мира остаются открытыми.

Прямой вызов генератора в завершённых чанках плоского тестового мира выявил
отсутствие служебной карты высот `OCEAN_FLOOR_WG`. Фикстура теперь заранее
создаёт эту карту, как обычный pipeline перед генерацией руд. В тесте обычного
мира подготовку карты выполняет сам Minecraft.

35 основных GameTests Chemical Laboratory проверяют:

- Все 24 самостоятельных рецепта реальными циклами по 100 тиков; количество,
  жидкость, стоимость энергии и обратный порядок там, где он разрешён.
- Восемь партий кислоты с выходом 8 000 mB, линейной стоимостью 16 000 FE и
  заменой BlockEntity в середине операции, у которой нет предметного результата.
- Шесть сторон передачи жидкости, запрет заполнения выхода, ёмкость входа,
  запрет смешивания, частичный отбор и транзакционный откат.
- Переключение отбора/сохранение двух баков; остановку перед последним платежом,
  несовместимый выход и завершение без дополнительного списания энергии.
- Правый клик вёдрами воды, кислоты и обычного молока с корректным остатком.
- Исходные настройки нефти/топлива, отключение лавового варианта, возвращение его
  через конфиг и объёмы альтернативных рецептов.
- Неравные переставленные количества, отсутствие флакона, неверную жидкость
  и нехватку одного mB. Все 31 записи рецептов проходят сетевые codecs.
- Полные FluidStack в меню: типы, 16 000 mB, компоненты жидкости и отказ пакета
  для другого контейнера. Проверены положение флакона, права владельца, кнопки
  очистки и отказ действий через закрытое меню.
- Разборку незавершённой партии с возвратом предметов и без создания ведра кислоты.
- Настоящие две лаборатории/воронки/печь: бревно → каучук → сырой пластик → пластина;
  обе стоимости энергии и воды оплачены. Отдельно кислота первой машины через
  fluid capability перерабатывает четыре руды в 12 yellowcake во второй.

Восемь основных тестов жидкостей проверяют источник/течение, отсутствие
бесконечных источников, ведро только из источника, частичный отказ и откат
переноса, плотность/вязкость, барьер плотности и боковое течение над водой.
Кислота наносит два пункта урона сквозь обычную броню, сохраняет hurt immunity
и не отбрасывает; молоко не наносит кислотного урона.

Отдельный `runChemistryTestServer` включает только изолированный тестовый пакет.
Он проверяет семь условных рецептов на тестовых представителях нефти/топлива/
угольной пыли/биотоплива и восьмой сценарий с включённым молоком NeoForge.
Это проверка правил интеграции, а не доказательство совместимости с каждым
сторонним модом. Пакеты `tests/chemistry-packs` не включаются в JAR.

Reaction Chamber: 30 основных сценариев и два условных.

- Все семь исходных реакций идут настоящими серверными тиками с контролем
  интенсивности; проверены расход одного реагента, жидкость и FE раз в 60 тиков.
- Четыре направления, все 36 обязательных блоков, три нижних и верхний разъёмы,
  транзакционный откат, ограничения слотов и отключение старых handles при разборке.
- Запись контроллера и всех деталей сохраняет прогресс, уровень, полные FluidStack,
  состояние RNG и связи. Отсутствующий контроллер не обслуживает старый handle;
  восстановленный с тем же UUID возобновляет доступ. Повреждённая запись расходуемой
  жидкости возвращает сырьё и начинает новый цикл без бесплатного результата.
- Снятый фокус, нехватка энергии, неправильные параметры, очистка бака, красный
  камень, разборка, полный выход и две исходные аварии. Сырьё не дублируется при
  разрушении контроллера, а готовая продукция не выпадает повторно.
- Меню проверяет владельца, текущий container ID, расстояние и неизвестные кнопки;
  значения до 1 млн FE переживают signed-short синхронизацию. Полные данные жидкости
  проходят сетевой codec. Это не проверка экранов с настоящими клиентами.
- Реальная цепочка камеры, воронки и печи превращает одну титановую руду в два слитка.
- Один BlockState стекла даёт разные коллизии для угла, стороны и центра;
  проверены исходные отступы 0,45 и высота верхнего корпуса 0,65. Это защищает
  позиционные формы от общего кеша BlockState Minecraft.
- Отдельный тестовый pack подставляет воду в теги жидких редстоуна/эндера:
  оба рецепта работают и отвергают лавовую замену при наличии предпочитаемой жидкости.

Радиация: девять сценариев. Проверены границы дозы, атрибут сопротивления,
исходные пороги симптомов, NPC-урон, инвентарное облучение, оба лекарства и возврат
тары, исключение радиации из очищения молоком, полная запись игрока и событие
клонирования через шину NeoForge. Тесты включают исходный серверный флаг локально
на время проверки; production-default остаётся выключенным. Перезапуск реального
сетевого игрока, костюмы, зоны, все зелья и весь радиационный интерфейс ещё требуют
дальнейшего переноса/приёмки. Подробности: [реакционная камера](REACTION_CHAMBER.ru.md).

Fabricator: 29 сценариев.

- Все шесть исходных рецептов проверяются настоящими тиками в одиночной и
  восьмикратной партии: 100 тиков и линейные 80×N FE/тик.
- Проверяются форма 2×2×2 во всех поворотах, исходная высота стекла 0,9,
  атомарность сборки и запрет присвоения деталей соседней установки.
- Три нижних корпуса предоставляют разъёмы энергии/предметов. У стекла разъёмов
  нет, жидкостного бака у Fabricator тоже нет. Транзакционный откат, ограничения слотов и старые handles
  после разборки/отсутствия контроллера проверены.
- Перезагрузка контроллера и деталей сохраняет партию и связи; разборка и
  нехватка питания/редстоун приостанавливают работу. Повреждённая запись UUID
  исправляет осиротевшие состояния без присвоения чужих ресурсов.
- Место в выходе и малейший запас ингредиента ограничивают партию. Неожиданная
  блокировка удерживает готовый результат без дополнительного расхода энергии.
- Разрушение после сохранения возвращает все четыре зарезервированных входа,
  включая пользовательское имя предмета; готовый результат не дублируется.
- Проверены codec, категории shift-click, владелец, расстояние, недопустимые
  кнопки и передача чисел до 100 000 FE через стандартную синхронизацию меню.
- Настоящая воронка переносит пустую ядерную ячейку из Fabricator в лабораторию,
  где расходуются вода и обогащённый уран. Проверены оба результата и точный расход
  8000 + 4000 FE. Первые кибердетали и весь цикл получения снаряжения всё ещё
  требуют переноса других систем. Подробности: [Fabricator](FABRICATOR.ru.md).

Лазеры: 25 специальных сценариев, два цикла в общей матрице и одна условная интеграция.

- ENERGY-урон в тик выстрела: 12 у винтовки, 9 у пистолета; ближайшая цель,
  запас хитбокса 0,3, стены/стекло, прохождение воды и сохранение блоков.
- Постоянный урон за 100 блоками, номинальная видимая длина промаха 100,
  полный bounding box, неподвижность и удаление на седьмом тике.
- Половина обычной брони, сопротивление ведьмы магии, отсутствие отбрасывания
  и учёт двух независимых попаданий в одном тике.
- Запись/чтение источника, конца и возраста луча без повторного попадания;
  отказ от неизвестного оружия, чужого типа снаряда, NaN/бесконечности и истёкшего срока.
- Отмена появления сущности не вызывает урон/расход/задержку; отмена попадания
  не повторяется после удаления обработчика. Реальный codec данных сущности
  переносит конец и возраст луча.
- Обе батареи возвращают один пустой корпус, расходуются только в конце R-перезарядки,
  полное оружие не создаёт лишние пустые корпуса.
- Четыре крафта оружия задают заряженный/пустой боезапас; полный цикл пистолета
  использует батарею из медных самородков/провода/редстоуна и её повторную зарядку.
- Ствол работает с золотом без электрума. Отдельный datapack заполняет тег электрума:
  золотой вариант перестаёт подходить, предпочтение сохраняется в сетевом codec рецепта.
  Тестовый заменитель электрума не входит в JAR. См. [LASERS.ru.md](LASERS.ru.md).

Зарядная станция: 30 сценариев.

- Два рецепта в партиях одна/восемь: длительность 62/25 тиков, 800 × N FE/тик,
  полная стоимость 49 600/20 000 FE на батарею. Ограничение партии свободным
  выходом, неизменность начатой партии при извлечении улучшений.
- Нехватка энергии и редстоун приостанавливают работу; завершённый выход ждёт
  освобождения без доплаты. Сохранение/разборка возвращают корректные предметы.
- Порты со всех сторон, транзакционный откат, ограничения слотов, рецепт станции,
  кодек рецептов, серверная проверка доступа и полные 100 000 FE в меню.
- Пакет рабочего предмета сохраняет имя/компоненты, очищается после завершения
  и не содержит полного инвентаря/владельца.
- Прямая зарядка: 1600 FE/тик, частичная подача, нулевая энергия, лимит приёмника,
  стек с суммарной ёмкостью больше Integer.MAX_VALUE, отказ приёмника и занятый выход.
- Частичный заряд переживает сохранение/разборку. Capability может заменить предмет
  другим типом без Energy capability: готовность переживает сохранение, а замена
  входа не наследует её. Настройка бесплатных рецептов не создаёт внешнюю энергию.
- Реальная цепочка Fabricator → воронка → Charging Station → R-перезарядка лазерной
  винтовки → выстрел: все предметы и расходы проверены.

Для прямого режима используются пять тестовых предметов с разными приёмниками;
они регистрируются только при `techguns.gametest=true`. Это не заменяет проверку
конкретных сторонних модов. Детали: [CHARGING_STATION.ru.md](CHARGING_STATION.ru.md).

Ракетница: 36 специальных сценариев и один общий цикл оружия.

- Все три фабрики: R-перезарядка, правильный тип патрона, скорости/сопротивление,
  TTL и сохранение; запрещены подмена семейства оружия и неизвестный вариант.
- Семь реальных рецептов, сохранение имени/патрона, исключение временных флагов,
  отсутствие дублирования загруженной ракеты; codec рецепта и компонента.
- Прямое попадание и hurt cooldown, падение прямого урона от начала полёта,
  растущая внешняя формула взрыва, радиусы трёх вариантов и тип EXPLOSION/броня.
- Безопасный/разрушающий удар в стену, сопротивление обсидиана, вода, TTL,
  неудачное прямое попадание, коэффициент отбрасывания и его отсутствие у чужого урона.
- Отмена spawn/impact/explosion, изменение списков блоков/целей обработчиком;
  безопасный режим не разрушает добавленные обработчиком блоки.
- Отсутствие внутреннего block loot, возврат содержимого сундука и запуск TNT callback.
- B, сохранение игрока, зарегистрированное право и OP-ограничение, снимок политики
  при выстреле, передача варианта снаряда клиенту.
- Ядерная зона: 20/22 тика, радиусы 15/25, исходные усилители, затухание,
  сохранение и 1200/1201 тиков; выключенная радиация и радиоактивность боеприпаса.

В первом прогоне одна проверка ожидала отбрасывание от ванильного `generic` damage,
который в этой версии его не создаёт. Проверка заменена обычным ударом игрока;
повторный полный запуск прошёл. Проверка OBJ также учитывает три исходные плоскости
корпуса: 120 реальных граней вместо 132 с вырожденными рёбрами.
Оригинальные FX, ручная проверка моделей и два реальных клиента остаются открытыми.
Подробности: [ROCKET_LAUNCHER.ru.md](ROCKET_LAUNCHER.ru.md).

SuperMutantBasic: 27 новых GameTests, шесть JUnit и шесть Python-проверок.

- Атрибуты, размеры, пять вариантов выбора четырёх типов оружия, настоящее яйцо
  и инициализация экипировки; загрузка не меняет имя, здоровье, руку или оружие.
- Все четыре типа снаряда, число дробин, yaw головы, бесконечные патроны NPC,
  безопасные обычные ракеты; отменённый спавн лазера не наносит урон.
- Реальные серверные тики AI: открытая/закрытая цель, очереди, попадания,
  переключение на ближний бой, запрет творческих/удалённых/далёких целей.
- Реальная ответная агрессия: свой класс игнорируется, чужой атакующий становится
  целью. Тест использует ServerLevel, который сам увеличивает tickCount.
- Множитель сложности и серверный DamageFactorNPC, отсутствие второго ванильного
  масштабирования, сохранение данных трёх семейств и совместимость старых сохранений.
- Пули, лазер, взрыв, кислота, радиация, fall и magic проверяют собственную броню
  NPC и сохранение настоящих ванильных обходов брони.
- Реальные loot tables: фиксированный ролл проверяет 75/80/90% с Looting; он не
  увеличивает количество кибердеталей, но увеличивает вторичные предметы.
- Смерть NPC с детерминированным seed → две кибердетали → исходный рецепт четырёх
  корпусов Fabricator. Все базовые биомы проверены на отсутствие придуманной
  естественной записи спавна SuperMutantBasic.
- Python проверяет иерархию, зеркальные части, исходную текстуру Basic, звуки,
  таблицу лута и извлечённые параметры AI. Внешний вид и обычная survival-доступность
  этим не подтверждаются. Подробности: [SUPER_MUTANT.ru.md](SUPER_MUTANT.ru.md).

## Что выявил запуск клиента

2026-09-09 по новой прямой просьбе пользователя «запускай» выполнен запуск текущего
клиента через командную строку. Журнал подтвердил создание OpenGL-контекста на
RTX 4060 и вход Dev в мир в 16:11 Europe/Zurich. Computer Use и управление вводом
не применялись; запущенная игра оставлена пользователю. Это разовый запуск по
просьбе, а не снятие ограничения `AGENTS.md` на автоматические клиентские проверки.

Загрузка выявила две ошибки, которые серверные тесты не обнаруживали:

- `lasergun`: у западной грани `Glow1` исходный U равен 0…−6 пикселей.
  Проверка прозрачности cuboid в 26.2 отклоняет выход за границу спрайта.
  Генератор переносит этот интервал в 64…58, сохраняя исходное повторение текстуры,
  направление и ширину. Интервалы через шов требуют разбиения грани и отклоняются
  явно; PNG остаётся исходным.
- `reactionchamber`: NeoForge 26.2 трактует `map_Kd` как имя texture slot.
  Вместо прямых идентификаторов в MTL подключены `#body`/`#glass` и соответствующие
  записи в JSON; исходные OBJ и PNG не меняются.

Добавлены две регрессионные Python-проверки и проверки границ UV/MTL-ссылок в
валидаторе ресурсов. Все 70 Python-проверок, генерация, ресурсный граф и `build`
прошли; обновлённый JAR собран. Ресурсы скопированы задачей `processResources`
в каталог уже запущенного клиента; пользователю предложено F3+T. В 16:18 клиент
завершился штатно (`BUILD SUCCESSFUL`), без новой записи о перезагрузке ресурсов.
Исправления будут применены при следующем запуске. Повторная клиентская загрузка
исправлений пока не подтверждена журналом; полная визуальная приёмка остаётся открытой.

Далее приведены наблюдения ранних запусков.

В первом запуске отсутствовали две текстуры: старые пути `guns/` и `items/`
не попадали в современный атлас. Генератор исправлен на `textures/item/` и
ссылки `techguns:item/...`; исходные PNG сохранены без изменения.

Полная визуальная перепроверка после исправления **не завершена**: пользователь
остановил Computer Use клавишей Escape и запретил дальнейшее управление рабочим столом.
Этот запрет сохранён в `AGENTS.md`; дальнейшие проверки выполняются без окон.
Пользователь затем сообщил, что оружие направлено обратно. Повороты обеих рук
исправлены, координаты UV при отражении пересчитаны. Направление ствола, сохранение
углов, диапазоны координат и соответствие UV исходным вершинам проверяются без UI.
Вид в creative, окончательные позиции в руках, звук в игровом мире и клиентские
анимации всё ещё требуют визуальной приёмки; она не заменяется математическими тестами.

В журнале также присутствует ошибка OSHI при чтении счётчиков Windows Perflib.
Она не помешала запуску окна Minecraft. Системные настройки не изменялись.

## Ограничения результата

- GameTest проверяет настоящий сервер Minecraft, но не заменяет тест двух клиентов
  по сети, reconnect, многопользовательскую синхронизацию или нагрузочное тестирование.
- Тики mock player в тестах перезарядки вызываются напрямую. Клавиатура, мышь,
  сетевые пакеты и анимация этими тестами не проверены.
- Клиент работал из dev source sets; установка собранного JAR в обычный launcher
  ещё не проверялась.
- Предыдущая базовая Linux CI успешно прошла: каталог, ресурсы, сборка, unit tests и GameTest server.
  [Запуск 34156681372](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34156681372)
  проверил коммит `4ad626eb27e40c43d1d8b3692b5b1d73bd406823`.
  Первый запуск выявил различие сортировки путей Windows/Linux; каталог исправлен
  на явную сортировку POSIX-путей, хеши нормализованы по LF.
- Семь образцов, модели и первая survival-цепочка также прошли Linux CI:
  [запуск 34161451780](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34161451780),
  коммит `aa9a224f9997bd6d0fc7b11fbee93ccf1a7f6336`.
- Формула защиты и расширенные 20 unit / 29 игровых тестов Techguns прошли Linux CI:
  [запуск 34161859463](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34161859463),
  коммит `7b991348689c71679fec2d79576be671b301b702`.
- Арсенал из 17 образцов, прицеливание, HUD, модели и 55 рецептов прошли чистую Linux CI:
  [запуск 34165584111](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34165584111),
  коммит `9d613d6fecf2d2bdfb54fd8076293b15b0cdbf4b`. Проверены генератор, 15 Python-тестов,
  сборка и 23 unit-теста, затем все 62 серверных GameTests. Клиентский HUD и модели
  здесь проверяются компиляцией/ресурсными проверками, а не отрисовкой.
- Ammo Press и актуальные 59 + 4 рецепта прошли чистую Linux CI:
  [запуск 34167760688](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34167760688),
  коммит `f810514d9a93fcee03d97b73ae6b72e5e628db61`. Все 19 Python-проверок,
  23 теста ядра и 79 серверных GameTests прошли; ошибок загрузки данных в журнале нет.
  Это подтверждает серверную часть пресса, но не его клиентскую отрисовку.
- Metal Press, общая основа обоих прессов и все 84 рецепта прошли чистую Linux CI:
  [запуск 34170140110](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34170140110),
  коммит `dd347ad1caacf19705be23b008072b0e5b04710a`. Прошли 23 Python-проверки,
  23 теста ядра и 108 серверных GameTests; ошибок загрузки данных в журнале нет.
  Модели и меню по-прежнему требуют отдельной визуальной приёмки.
- Blast Furnace и передача полных чисел меню прошли чистую Linux CI:
  [запуск 34171996202](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34171996202),
  коммит `be101ead94fe37140cfe81fc6c71f9739a3b8dff`. Прошли 26 Python-проверок,
  23 теста ядра и все 123 серверных GameTests. Все 89 рецептов и ресурсы проверены;
  ошибок загрузки данных в журнале нет. Клиентская визуальная приёмка остаётся открытой.
- Пять руд, все 19 рецептов обычной печи и генерация мира прошли чистую Linux CI:
  [запуск 34173753444](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34173753444),
  коммит `284e877f779bae63f745399f6f253d951c9150fe`. Проверены 584 сгенерированных
  файла, 32 Python-теста, сборка и 23 теста ядра, 154 основных серверных GameTests
  и отдельный тест генерации 16 новых чанков обычного мира. Все 113 рецептов
  и 91 item definition проверены; ошибок в журналах обоих игровых запусков нет.
  Визуальная приёмка, несколько seed, химическая переработка и остальные этапы
  полного переноса остаются открытыми.
- Chemical Laboratory и жидкости прошли чистую Linux CI:
  [запуск 34328096044](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34328096044),
  коммит `3126d100af4bd0591c54c56a10d46a452a47a444`. Проверены 736 сгенерированных
  файлов, 38 Python-тестов, сборка и 23 теста ядра. Прошли 197 основных GameTests,
  восемь дополнительных химических и один тест новых чанков — всего 206.
  Все 154 рецепта и 116 item definitions проверены; ошибок в игровых журналах нет.
  Это проверка предыдущего химического среза. Радиация и камера проверены отдельно ниже.
- Reaction Chamber и основа радиации прошли чистую Linux CI:
  [запуск 34334349814](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34334349814),
  коммит `2fa9bdef7dc94619307f4b89a2a3081bc57caeb5`. Проверены 822 сгенерированных
  файла, 44 Python-теста, сборка и тесты ядра. Прошли 235 основных GameTests,
  десять условных и один тест новых чанков — всего 246. Все 169 рецептов,
  127 item definitions, 183 ссылки на текстуры и 40 звуковых событий проверены;
  в полном журнале нет `/ERROR]`. Мировой рендер луча/жидкости/предмета, первичные
  источники кибернетических деталей, остальные системы и визуальная приёмка остаются.
- Полный перенос и соответствие всем механикам оригинала не заявляются.

Fabricator и общая основа многоблоков прошли чистую Linux CI:
[запуск 34345232962](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34345232962),
коммит `c1bb0b9c8515126757d982cb98a876cf20cf63f7`. Подтверждены 882 сгенерированных
файла, 50 Python-тестов, сборка/тесты ядра и все 276 игровых проверок:
265 основных, десять условных и одна генерации мира. Проверены 179 рецептов,
135 item definitions, 200 ссылок на текстуры и 41 звуковое событие. В полном
журнале нет `/ERROR]`. Рабочие анимации, рендер предмета внутри Fabricator,
первые кибернетические детали через исходных мобов и полный перенос остаются открытыми.

Локальные машинные отчёты: `core/build/test-results/test/`,
`core/build/reports/tests/test/` и журналы в `platforms/neoforge-26.2/run/logs/`.
Сырые логи и тестовые миры игнорируются Git.

После исправления атласа проверено содержимое JAR: в нём есть классы общего ядра,
современные item definitions и PNG в `textures/item/`; классов legacy-оружия нет.
Ссылки моделей, текстур, имён и звуков проверяет `tools/verify_resources.py`.

Лазерная винтовка/пистолет, мгновенные лучи и зеркальные детали моделей прошли
чистую Linux CI: [запуск 34351592180](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34351592180),
коммит `a6cb826bc9b664c072236d7e0d0c7565572300b9`. Подтверждены 922 сгенерированных
файла, 56 Python-проверок, сборка/тесты ядра и 304 игровых сценария:
292 основных, 11 условных и одна генерации мира. В ресурсном графе 141 предмет,
208 ссылок на текстуры моделей, 45 звуковых событий и 186 рецептов.
Итоговый `headSha` CI совпадает с отправленным коммитом; в полном журнале нет `/ERROR]`.
Windows отдельно прошёл 32 JUnit-теста без ошибок/пропусков и те же игровые наборы.
Итоговый JAR содержит код/модели/текстуры/рецепты/теги лазеров и исходную лицензию;
legacy-классы и тестовый datapack внешнего электрума в него не попадают.
GPU-отрисовка, ручная приёмка и полный Survival-путь остаются ограничениями,
описанными в [LASERS.ru.md](LASERS.ru.md).

Зарядная станция прошла чистую Linux CI:
[запуск 34355548852](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34355548852),
коммит `f579d1f14f5cb393ebf4d2346521b70d1f4f2766`. Подтверждены 937 сгенерированных
файлов, 62 Python-проверки, сборка/тесты ядра и все 334 игровых сценария:
322 основных, 11 условных и одна генерации мира. Ресурсный граф содержит
142 item definitions, 212 ссылок на текстуры моделей, 46 звуковых событий и 189 рецептов.
Завершённый запуск CI имеет ожидаемый `headSha`; в полном журнале нет `/ERROR]`.
Windows отдельно подтвердил 32 JUnit-теста без ошибок/пропусков и те же игровые наборы.
Проверен итоговый JAR: код станции/рендера, модели, оригинальные PNG/OGG, рецепты
и лицензия присутствуют; legacy-классов и условного datapack в нём нет.
GPU-приёмка станции и проверка конкретных внешних энергетических предметов остаются
открытыми; источник первых кибердеталей и проверка внешнего питания ещё остаются.

Текущий срез ракет проверен локально: 988 сгенерированных файлов, 68 Python,
36 JUnit, 359 основных GameTests, 11 условных и один тест обычного мира.
Ресурсный граф: 148 item definitions, 229 ссылок на текстуры, 49 звуков и 196 рецептов.
JAR содержит код, модели, исходные текстуры/звуки, рецепты/теги ракет и лицензию;
legacy-классов и условных тестовых datapack в нём нет. Дерево legacy не изменено.
Linux CI ракетницы завершён успешно:
[запуск 34361077262](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34361077262),
коммит `cf8bc7757fc3c5ee8ecaf5d216fcd443ea90341c`. Проверены генерация ресурсов,
68 Python-проверок, сборка/ядро, все 359 основных GameTests, один обычного мира
и 11 условных. `headSha` завершённого запуска совпадает с отправленным кодом;
полный журнал не содержит `/ERROR]`. Последующий коммит меняет только документацию.

Исправления ресурсов после запуска клиента прошли Linux CI:
[запуск 34362746206](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34362746206),
коммит `aadd217f262f3844c0d7a7cfa662855533a579da`. Подтверждены 70 Python-проверок,
генератор, ресурсный граф с 231 ссылкой на текстуры, сборка/ядро и все 371 GameTest.
Завершённый запуск имеет ожидаемый `headSha`; в полном журнале нет `/ERROR]`.
Три исправленных ресурса отдельно сверены с содержимым локального JAR.
Это не подтверждение повторной GPU-загрузки: клиент завершился до F3+T и не
перезапускался автоматически.

Первый NPC SuperMutantBasic прошёл Linux CI:
[запуск 34603679914](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34603679914),
коммит `6af4e8640cf052feedf830b269bd9c61082ec9a8`. Запуск завершён успешно
2026-09-11 в 15:23 Europe/Zurich. Проверены 1006 сгенерированных файлов,
76 Python-проверок, сборка/тесты ядра и все 398 игровых сценариев:
386 основных, 11 условных и один генерации мира. Ресурсный граф содержит
149 item definitions (146 предметов и три виртуальные модели летящих ракет),
231 ссылку на текстуры моделей, 53 звуковых события и 196 рецептов.
`headSha` завершённого запуска совпадает с отправленным кодом; полный журнал
не содержит `/ERROR]`. Windows отдельно подтвердил 42 JUnit-теста без ошибок
и пропусков и те же игровые наборы. Итоговый JAR содержит код NPC, модель,
исходные текстуру/звуки, яйцо, loot table и лицензию; legacy-классов и тестовых
datapack в нём нет. Дерево исходного `legacy/1.12.2/src` не изменено.
Следующий коммит фиксирует только этот отчёт и статус проверки.
Естественный survival-источник кибердеталей и GPU-приёмка NPC остаются открытыми;
клиент для этого этапа не запускался. См. [SUPER_MUTANT.ru.md](SUPER_MUTANT.ru.md).

CyberDemon и Nether Blaster проверены локально 2026-09-11 в 18:47–18:48 Europe/Zurich.
Добавлены 26 специальных GameTests и общий цикл нового оружия, четыре JUnit и шесть
Python-проверок. Итог: 46 JUnit, 82 Python и 425 GameTests (413 основных, 11 условных,
один обычного мира). Сборка и все наборы завершились успешно; `/ERROR]` в журнале нет.

- Реальный `NaturalSpawner` в Nether с исходными весами 100/30 создаёт CyberDemon.
  Контролируемая тёмная площадка проверяет обычный путь Minecraft через таблицу
  биома, поверхность, свет, расстояние до игрока и замену TGDummySpawn.
  Затем реальная смерть даёт две кибердетали по фиксированному seed, и они используются
  в исходном рецепте четырёх корпусов Fabricator.
- Отдельно проверены пять Nether-биомов, общий вес 300/группа 1–3, blacklist,
  отсутствие прямой записи CyberDemon и спавна селектора в Overworld. Варианты
  неперенесённого ZombiePigmanSoldier и нулевая таблица не создают кибердемона.
- Проверены FIRE-урон, обычная/врождённая броня, огнестойкость, сопротивление ведьм,
  повторные попадания, живой множитель NPC, стены и отсутствие поджигания/разрушения.
  Снаряд сохраняет источник дистанции, NPC-контекст и оставшееся время; неверные типы,
  неполные координаты, NaN/бесконечности и координаты вне мира не принимаются.
- Проверены исходные торможение в воздухе/воде, 60 движений до истечения TTL,
  расстояние до начала тика столкновения и события отмены появления/попадания.
  Живой AI, исходная точка выстрела, бесконечные патроны NPC, нежить, имя/экипировка,
  отсутствие агрессии между двумя членами HOSTILE и три пула лута проходят проверки.
- Генератор подтверждает 1030 файлов; ресурсный граф содержит 152 item definitions,
  234 ссылки на текстуры, 55 звуковых событий и 198 рецептов. Пустое событие
  перезарядки бластера отражает отсутствие записи в исходном sounds.json.
- Итоговый JAR — 4 690 035 байт: проверены классы NPC/снаряда/спавна/моделей,
  исходная лицензия и побайтовое совпадение всех 1005 сгенерированных ресурсов
  основного мода. Legacy-классы и изолированные тестовые datapack отсутствуют;
  исходное дерево 1.12.2 не изменено.

Linux CI завершён успешно 2026-09-11 в 18:59 Europe/Zurich:
[запуск 34624828905](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34624828905),
коммит `d4b5e7c804acbed996dc98cbb123f55e2dfe6859`. Проверены генератор/ресурсный граф,
82 Python-проверки, сборка/ядро и все 425 GameTests: 413 основных, один обычного
мира и 11 условных. `headSha` завершённого запуска совпадает с отправленным кодом;
в полном журнале нет `/ERROR]`. Последующий коммит меняет только документацию:
фиксирует результаты CI и уточняет исходное вооружение/броню следующего NPC.

На момент этого среза исходные FX, визуальная приёмка, ZombiePigmanSoldier,
спавнеры, остальные NPC и проверка внешнего питания остаются открытыми.
См. [CYBER_DEMON.ru.md](CYBER_DEMON.ru.md).

## Клиентский запуск CyberDemon — 2026-09-11

По прямой просьбе пользователя запущен клиент с коммитом
`173dc503cfce446e5a8ec075f85d1bdc68b17572`: OpenGL и звук инициализированы,
Dev вошёл в мир в 19:03:26 Europe/Zurich, штатное завершение — в 19:04:44.
После выхода пользователь подтвердил, что можно продолжать разработку.
Запуск выполнялся через команду, без Computer Use и управления окнами/вводом.
Журнал сохранён локально в `.tools/client-20260911-190245.runtime.log`.
Единственная запись ERROR относится к уже известному счётчику Windows OSHI/Perflib;
ошибок загрузки ресурсов в журнале нет. Это запуск предыдущего среза:
новая броня T2 и зомби-пигман в нём ещё отсутствовали.

## T2 Combat Armor и ZombiePigmanSoldier — 2026-09-11

На Windows полный запуск завершился успешно в 19:51–19:52 Europe/Zurich:
**52 JUnit, 88 Python и 456 GameTests** — 444 основных (443 Techguns + один
встроенный), 11 условных и один генерации обычного мира. В итоговом журнале
сборки/игровых тестов нет `/ERROR]`. Проверка выполнялась без Minecraft-клиента.

- Добавлен **31 GameTest**: 21 для брони и десять для солдата. Отдельно добавлены
  шесть тестов ядра и шесть проверок извлечения исходников/генерации ресурсов.
- Проверены дробная физическая/пулевая/FIRE/POISON-защита, исходное пробитие
  игрока, смешанная ванильная броня, fall без износа, два этапа износа и слом.
  Нулевой урон не изнашивает вещь; отмена урона/ArmorHurtEvent учитывается;
  отдельное повреждение слота сохраняет нативный путь. Mixin заменяет только
  обычный вызов `Player.hurtArmor`, который иначе повторно списывал прочность.
- Четыре части имеют исходные 990 прочности и toughness 1. Проверены шесть
  камуфляжей, сохранение и настоящий сетевой codec ItemStack, Shift + ПКМ,
  ремонт слитком обсидиановой стали с сохранением имени/камуфляжа, отказ ткани,
  объединение повреждённых вещей и зачарованная книга. Проверены бонусы
  ходьбы/спринта/прыжка/отбрасывания, изношенные вещи и снятие перед ударом.
- Проверены все девять вариантов исходного выбора оружия солдата, постоянный
  шлем T2 с camo=3 и независимая выдача остальных частей. Четыре оружия стреляют
  настоящими снарядами; работают живой AI, бесконечные патроны, атрибуты/нежить,
  яйцо и NBT. Игроковые бонусы T2 не переходят на NPC.
- Шесть независимых пулов лута проверены с Looting и без него. Реальная смерть
  солдата даёт слиток, которым ремонтируется T2. Существующий NaturalSpawner-тест
  расширен: исходная таблица 100/30 теперь создаёт **обоих** Nether-NPC.
- Генератор подтверждает **1083 файла**. Ресурсный граф содержит **158 item
  definitions, 239 ссылок на текстуры моделей, 55 звуковых событий и 203 рецепта**.
  Дополнительно проверены шесть equipment definitions и 12 исходных слоёв брони;
  ванильные текстуры пиглина не копируются в форк.
- Итоговый JAR — **4 855 703 байта**. Проверены 11 обязательных классов/служебных
  файлов, исходная лицензия и побайтовое совпадение **1055 сгенерированных ресурсов**.
  Legacy-классы и изолированные тестовые datapack отсутствуют. Дерево исходников
  `legacy/1.12.2/src` осталось `270b27b1cc52019733bb79facda68ce29aa038be`.

Linux CI завершён успешно 2026-09-11 в 20:09 Europe/Zurich:
[запуск 34631389869](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34631389869),
коммит `7d8beb8140ad83cb9c143324e29cf0eb8f4f008e`. Подтверждены 1083 сгенерированных
файла, 88 Python-проверок, ресурсный граф, сборка/тесты ядра и все 456 GameTests:
444 основных, один обычного мира и 11 условных. `headSha` завершённого запуска
совпадает с отправленным кодом; в полном журнале нет `/ERROR]`.
Следующий коммит меняет только документацию: фиксирует CI и конкретизирует
следующий этап Repair Bench по исходникам.

Визуальная приёмка T2/солдата, Repair Bench,
остальные комплекты/NPC, спавнеры, внешнее питание и полный перенос остаются открытыми.
Подробности: [T2_ARMOR_PIGMAN.ru.md](T2_ARMOR_PIGMAN.ru.md).


## Клиентский запуск T2/солдата — 2026-09-12

По прямой просьбе пользователя запущен коммит
`b6940fb84a8f4144c070ee5fabe265d0f040a228`: Dev вошёл в мир в 10:45:31
Europe/Zurich, клиент завершился штатно в 10:46:41. Пользователь сообщил, что
проверил сборку и в целом всё нормально, после чего попросил продолжить разработку.
Использован только запуск командой, без Computer Use, управления окнами и вводом.
В журнале нет ошибок ресурсов; остаётся известная ошибка Windows OSHI/Perflib.
Локальная копия: `.tools/client-20260912-104307.runtime.log`. Этот запуск не
содержал нового Repair Bench; детальная матрица визуальной приёмки остаётся открытой.

## Repair Bench — 2026-09-12

На Windows прошли **54 JUnit, 92 Python и 484 GameTests**: 472 основных
(471 Techguns + один встроенный), 11 условных и один генерации обычного мира.
Сборка/основной набор завершились в 11:04, остальные игровые наборы — в 11:05
Europe/Zurich. В обоих итоговых игровых журналах нет `/ERROR]`.
Полный повторный Python-набор завершился в 11:10: 92 проверки за 82,348 секунды.

- Добавлены 28 GameTests, два JUnit и четыре Python-проверки. Исправлен ожидаемый
  размер общего тега кирок в прежнем тесте руд: 17 блоков после добавления станка.
- Проверены исходный рецепт с metadata=9 и современным тегом верстаков,
  поворот/полный куб, отсутствие энергии/жидкости и шесть целей ремонта.
- Нехватка ткани отменяет уже выполненное извлечение металла. Материалы из
  разных слотов и с именами учитываются; обычный инвентарь игрока не расходуется.
  Неповреждённые/неподдерживаемые вещи, повторные и недопустимые запросы не списывают материалы.
- Ремонт сохраняет имя, камуфляж, equipment asset, зачарования и штраф наковальни,
  не тратит XP и восстанавливает бонусы T2. Смена вещи перед нажатием не использует
  устаревшую стоимость. Проверены границы округления и полная стоимость четырёх частей.
- Все кнопки/слоты проверяют владельца, актуальный доступ, открытое меню,
  расстояние и наличие блока. Проверены наблюдатели, проклятие несъёмности,
  переполненный инвентарь, крайние индексы shift-click и захват бесхозного станка.
- Проверены сохранение владельца/материалов/брони до и после ремонта,
  настоящие пакеты предметов и двух значений меню, все шесть портов и несайдовое
  обращение, откат/фиксация транзакций. Настоящие воронки подают материалы и
  забирают отремонтированную броню. Разборка возвращает блок и содержимое один раз.
- Генератор подтверждает **1097 файлов**. Ресурсный граф: **159 item definitions,
  253 ссылки на текстуры моделей, 55 звуковых событий и 204 рецепта**.
  Исходные PNG блока и GUI сверены побайтово; теги кирок/материалов объединены.
- JAR — **4 910 052 байта**. Проверены пять классов нового станка/экрана,
  лицензия, Mixin-конфигурация и все **1068 сгенерированных ресурсов** основного мода.
  Legacy-классы, тестовые datapack и текстуры Minecraft в него не попадают.
  Дерево `legacy/1.12.2/src` осталось `270b27b1cc52019733bb79facda68ce29aa038be`.

Linux CI завершился успешно 2026-09-12 в 11:14 Europe/Zurich:
[запуск 34685217430](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34685217430),
коммит `06c2ba077c0faf50156deb7c5f6a9761741ff3d1`. Подтверждены 1097 сгенерированных
файлов, 92 Python-проверки, ресурсный граф, сборка/ядро и все 484 GameTests:
472 основных, один обычного мира и 11 условных. `headSha` завершённого запуска
совпадает с отправленным коммитом; в полном журнале нет `/ERROR]`.
Последующий коммит меняет только документацию: фиксирует CI и уточняет следующий
срез Camo Bench по исходникам.

Новый интерфейс ещё требует клиентской приёмки;
проверка двух реальных клиентов, Camo Bench, остальные комплекты/щиты, NPC,
структуры и проверка внешнего питания остаются частью полного плана. Подробности:
[REPAIR_BENCH.ru.md](REPAIR_BENCH.ru.md).


## Camo Bench и общая основа станков — 2026-09-12

На Windows прошли **59 JUnit, 97 Python и 519 GameTests**: 507 основных
(506 Techguns + один встроенный), 11 условных и один генерации обычного мира.
Основной набор завершился в 11:41, остальные игровые наборы — в 11:44 Europe/Zurich.
Полный Python-набор прошёл за 96,672 секунды. В итоговых игровых журналах нет `/ERROR]`.

- Добавлены 35 GameTests, пять JUnit и пять Python-проверок. В прежнем тесте
  объединения тегов кирок учтено добавление 18-го блока. Совпадающая боковая
  текстура станков разделяется, разные данные по одному пути отклоняются.
- Все восемь ванильных палитр проверены на полном круге в обе стороны:
  128 реально зарегистрированных предметов, правильный порядок, количество,
  имя и обновление нативного item model. Шесть камуфляжей T2 проходят циклы
  во входе и в каждом надетом слоте с сохранением износа/зачарований.
- У знамени сохраняются фигуры, цвета и порядок узоров, имя и custom data.
  Реальная установка перекрашенного знамени подтверждает цвет основы и узоры
  в BannerBlockEntity. Порядок старых dye metadata сверён по официальному
  клиенту 1.12.2 и таблице классов Forge 2807; эти файлы в форк не добавляются.
- Проверены текущее содержимое при смене предмета, последовательные действия
  двух серверных представителей игроков и независимость надетой брони.
  Проверены все кнопки/слоты при закрытом или другом меню, потере доступа,
  удалении блока, превышении расстояния и режиме наблюдателя.
- Проверены точные пакеты предметов/двух значений меню, сохранение палитры,
  узоров, брони и владельца; 42 слота; пустой вход, слияние/переполнение,
  возврат брони, вторая рука, проклятие несъёмности и полный инвентарь.
- Все стороны/несайдовое обращение поддерживают предметные транзакции;
  энергетического и жидкостного порта нет. Настоящие воронки передают стек
  через перекраску. Разборка возвращает блок/перекрашенное содержимое один раз.
- Общая основа `OwnedWorkbenchBlock`/`OwnedWorkbenchBlockEntity`/`WorkbenchMenu`
  сохраняет прежние ключи и десять слотов Repair Bench. Его 28 существующих
  GameTests проходят; дополнительная проверка подтверждает сохранение и ремонт
  после выделения общей логики. Исходный расчёт материалов не изменён.
- Генератор подтверждает **1110 файлов**. Ресурсный граф: **160 item definitions,
  267 ссылок на текстуры моделей, 55 звуковых событий и 205 рецептов**.
  Исходные текстуры/GUI сохранены побайтово, таблицы metadata→ID записаны в каталоге.
- JAR — **4 965 318 байт**: проверены 12 классов станков/общей основы/ядра,
  лицензия и Mixin-конфигурация, все **1079 сгенерированных ресурсов** основного мода.
  Legacy-классы, изолированные тестовые datapack и текстуры Minecraft отсутствуют.
  Дерево `legacy/1.12.2/src` осталось `270b27b1cc52019733bb79facda68ce29aa038be`.

Linux CI завершился успешно 2026-09-12 в 11:53 Europe/Zurich:
[запуск 34686875653](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34686875653),
коммит `31aa48fc3d781a1bcbed9053e85d92807cda1541`. Подтверждены 1110 сгенерированных
файлов, 97 Python-проверок, ресурсный граф, сборка/тесты ядра и все 519 GameTests:
507 основных, один обычного мира и 11 условных. `headSha` завершённого запуска
совпадает с отправленным коммитом; в полном журнале нет `/ERROR]`.
Отдельная локальная сверка всех 16 констант старого EnumDyeColor подтвердила
таблицы всех восьми палитр. Последующий коммит меняет только документацию.

Клиентский предпросмотр/интерфейс, два реальных
клиента, декоративные блоки Techguns и дополнительные слоты ещё требуют переноса
или приёмки. Подробности: [CAMO_BENCH.ru.md](CAMO_BENCH.ru.md).

Исправлена прежняя ошибка плана об энергетике: `TGConfig.java:168` прямо
предусматривает генераторы другого мода либо `machinesNeedNoPower`; руководство
`guide_tier0.json` описывает тот же путь. Собственного электрогенератора исходный
мод не содержит. Проверка внешнего FE-питания остаётся задачей интеграции;
фиктивный перенос собственных генераторов больше не включается в объём.


## Grinder — 2026-09-12

Финальная локальная сборка и три серверных запуска завершились в 12:38 Europe/Zurich:
**65 JUnit, 104 Python, 577 GameTests** — 564 основных (563 Techguns + один встроенный),
12 условных и один тест обычного мира. Добавлены **57 основных и один условный**
сценарий Grinder, шесть JUnit и семь Python-проверок.

- Все 20 оружейных рецептов и девять рецептов патронов выполняются на настоящих
  серверных тиках. Проверены пустое/заряженное оружие, без возврата его боеприпасов;
  исходные количества, коэффициенты 1,5 и пакетный расход 500 FE на предмет.
- Для каждой части T2 проверены повреждения 0, 1 и 989. Формула сохраняет
  дополнительное округление здоровой брони; отдельные JUnit проверяют пороги.
- Проверены выбор партии по вместимости, частично занятые/забитые выходы,
  лимит 64 при компонентах со стеком 99, паузы питания/редстоуна,
  сохранение заранее определённого результата и отсутствие повторных бросков.
  Завершённая заблокированная партия после перезагрузки не тратит энергию.
- При разборке проверены возврат исходного предмета с компонентами для
  незавершённого цикла, готовых материалов для оплаченного и один блок станка.
  Проверены транзакции всех сторон/unsided, семь улучшений, девятый выход,
  реальные воронки, приватный/устаревший/удалённый инвентарь и shift-click.
- Native menu packets передают два поля доступа и семь показателей станка.
  Render update передаёт рабочую вещь/прогресс без скрытого случайного результата.
  Recipe codec сохраняет количества, коэффициенты и preferred_tag. Условный
  datapack подтверждает электрум вместо золотого fallback, без подключения
  реального стороннего мода. Поддержка остальных модов остаётся непроверенной.
- Исходные ItemStack metadata и боезапас перепроверены отдельно: Forge
  Item.getMetadata(ItemStack) возвращает damage, но GenericGun после создания
  сбрасывает его в ноль и хранит изменяемый боезапас в NBT. Поэтому различный
  боезапас не блокирует разборку. Износ брони инвертируется без верхнего clamp.
- Побайтово сверены исходные PNG/OGG, 19 элементов модели блока и 21 предмета;
  четыре грани каждого вала сохраняют размеры и направление UV. Скомпилированы
  GUI, противоположное вращение валов и опускание рабочей вещи. Клиент не запускался.
- Генератор подтверждает **1168 файлов**; ресурсный граф — **162 item definitions,
  283 ссылки на текстуры, 57 звуковых событий и 240 рецептов**: 114 верстака,
  19 обычной печи и 107 машинных. Реальных предметов — 158, машин — десять.
- Итоговый JAR — **5 156 736 байт**. Проверены десять обязательных классов/служебных
  файлов, лицензия и побайтовое совпадение **1136 сгенерированных ресурсов JAR**.
  В нём нет legacy-классов, тестовых datapack или скопированных текстур Minecraft.
  Дерево legacy осталось `270b27b1cc52019733bb79facda68ce29aa038be`.

До итогового прогона исправлены конструктор ItemParticleOption под 26.2,
ограничение 64 против современного default 99 и верхняя граница GUI-прогресса
для завершённой заблокированной операции. Финальный журнал не содержит ERROR.
Linux CI завершён успешно 2026-09-12 в 12:47 Europe/Zurich:
[запуск 34689193608](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34689193608),
коммит `d8976f26d769ffd2136fa048a43ebaa818c9e532`. Подтверждены 1168 сгенерированных
файлов, 104 Python-проверки, ресурсный граф, сборка/тесты ядра и все 577 GameTests:
564 основных, один обычного мира и 12 условных. `headSha` завершённого запуска
совпадает с исходным коммитом; все шаги задания успешны, в игровых логах нет ERROR.
Последующий коммит меняет только документацию и фиксирует это подтверждение. Клиентская приёмка Grinder, два реальных клиента, остальные рецепты
оружия/брони и интеграции ещё открыты. См. [GRINDER.ru.md](GRINDER.ru.md).


## Hazmat и общая основа брони — 2026-09-12

Финальный Windows-прогон завершился в 13:13 Europe/Zurich: **71 JUnit,
111 Python и 623 GameTests** — 610 основных (609 Techguns + один встроенный),
12 условных и один тест обычных чанков. Добавлены **46 GameTests**, шесть JUnit
и семь Python. Исходники `legacy/1.12.2` не менялись.

- Четыре части Hazmat и protectivefiber имеют исходные рецепты: 24 волокна на
  комплект, три волокна из ткани/резины/свинцовой пластины. Перенесены 1100 прочности,
  дробные защиты, нулевая toughness/enchantability, четыре расцветки и бонус ботинок.
- Типовая защита проверена реальными попаданиями: физический, FIRE-бластер,
  кислота/POISON, взрыв, радиация, пуля AK-47 с исходным пробитием 0,5.
  Общая математика также проверяет DARK, ICE, LIGHTNING и UNRESISTABLE.
- Атрибут радиационного сопротивления по +1 на часть работает для игрока и моба,
  остаётся при повреждении 1099 и складывается с таблетками. Проверены все размеры
  частичного комплекта, радиоактивный инвентарь, более сильное воздействие,
  неправильный слот, снятие/разрушение, повторные обновления и сохранение игрока.
  Костюм не лечит уже накопленную дозу и не блокирует урон от отравления.
- Специальный износ чистого Hazmat останавливается на 1099. В смешанном комплекте
  T2 запускает обычный второй проход, который способен сломать изношенный Hazmat.
  Смешанный RADIATION-урон отдельно проверяет оба прохода и правильные количества
  износа. Cancelled attack/ArmorHurtEvent не расходуют прочность преждевременно.
- Для каждой части проверены ремонт за protectivefiber, четыре камуфляжа и
  предметные NBT/network codecs; наковальня, парный ремонт и книга Mending.
  Двенадцать настоящих циклов Grinder проверяют повреждения 0, 1, 1099 у всех
  частей. Дополнительно проходит Repair Bench → Camo Bench → Grinder.
- Исходная формула падения меняет дистанцию: max(0, distance − 0,5) × 0,9.
  Сверены старый байткод EntityLivingBase из официального клиента 1.12.2 (ceil)
  и LivingEntity.calculateFallDamage 26.2 (floor с epsilon). Движок 26.2 сохранён:
  дистанция 10 с ботинками даёт 5 урона, с изношенными — 7. Первое ожидание теста
  было исправлено после этой сверки; формула Techguns не менялась под тест.
- Общие TGArmorItem/TGArmorSystem заменили имена T2-only классов; IDs, компоненты,
  текстуры и параметры прежних T2 сохранены. Все старые игровые проверки проходят.
- Генератор подтверждает **1205 файлов**, ресурсный граф — **167 item definitions,
  288 ссылок на текстуры, 57 звуковых событий и 249 рецептов**: 119 верстака,
  19 обычной печи, 111 машинных. Восемь частей брони, 70 материалов, 163 предмета,
  десять станков; equipment definitions — десять, исходных текстур слоёв — двадцать.
- JAR — **5 231 401 байт**. Проверены 11 обязательных классов/служебных файлов,
  исходная лицензия и побайтовое совпадение **1172 сгенерированных ресурсов JAR**.
  Старые T2-only классы не остались в архиве. Нет legacy-классов, тестовых datapack
  или копий текстур Minecraft. Дерево legacy: `270b27b1cc52019733bb79facda68ce29aa038be`.

Linux CI завершён успешно 2026-09-12 в 13:26 Europe/Zurich:
[запуск 34690903280](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34690903280),
коммит `b21067099a1036e58cf985eded12aa975f476d32`. Подтверждены 1205 сгенерированных
файлов, 111 Python-проверок, ресурсный граф, сборка/тесты ядра и все 623 GameTests:
610 основных, один обычного мира и 12 условных. `headSha` совпадает с исходным
коммитом, все шаги задания успешны; в игровых логах нет ERROR. Последующий коммит
фиксирует только документацию этого подтверждения.
Клиент в этом этапе не запускался. Приёмка внешнего вида, два реальных клиента,
другие комплекты и специальные брони сторонних модов остаются открытыми.
Следующий срез — T1 Combat, затем ZombieSoldier/Overworld-спавн.
Подробности: [HAZMAT.ru.md](HAZMAT.ru.md).


## T1 Combat — 2026-09-12

Финальная Windows-проверка завершилась в 13:41 Europe/Zurich: **76 JUnit,
117 Python и 658 GameTests** — 645 основных (644 Techguns + один встроенный),
12 условных и один тест обычных чанков. Добавлены **35 GameTests**, пять JUnit
и шесть Python-проверок; прежние T2/Hazmat и остальные системы проходят.

- Каждая из четырёх частей T1 имеет исходные 825 прочности, toughness 0,5,
  дробную типовую защиту и бонус отбрасывания. Реальные попадания проверяют
  физический, FIRE, POISON и пулевой урон, включая raw penetration 0,5.
  Проверены смешанный комплект T1/T2/Hazmat и два прохода износа/разрушение.
- Нативный knockback подтверждает уменьшение силы до 0,6 с полным комплектом
  и полную силу при повреждении 824. Отдельные проверки исключают бонусы
  скорости/радиации и утечку бонусов игрока на NPC. HUD показывает 4+5+4+3.
- Для всех частей загружаются исходные рецепты: 11 обычного железа и 13 ткани
  на комплект. На наковальне железо восстанавливает 206 прочности; проверены
  имя, парный ремонт, Mending и отказ при неподходящем материале.
- Для каждой части проверен Repair Bench, включая точную стоимость и
  сохранение имени; нехватка ткани откатывает извлечение железа. Все 12 сочетаний
  часть × повреждение 0/1/824 проверены настоящими тиками Grinder: обычное железо,
  heavycloth, сохранённое округление здоровой брони и 500 FE на предмет.
- T1 — одноцветный GenericArmor, без ICamoChangeable. Серверные запросы перекраски
  входной/надетой T1 отклоняются, Camo Bench не показывает фиктивный номер варианта.
  Shift + ПКМ надевает вещь. Проверки tooltip исключают ложные подсказки скорости
  и камуфляжа; T2 продолжает показывать свой реальный бонус скорости.
- Генератор научился читать вложенный ItemStack в setRepairMats и необязательные
  бонусы GenericArmor; ванильный ID железа разрешается фабрикой брони. Сверены
  четыре оригинальные иконки, две текстуры экипировки, локализация и каталог.
- Генератор подтверждает **1229 файлов**, ресурсный граф — **171 item definitions,
  292 ссылки на текстуры, 57 звуковых событий и 257 рецептов**: 123 верстака,
  19 обычной печи, 115 машинных. Три комплекта/12 частей брони, 70 материалов,
  167 предметов, десять станков; 11 equipment definitions и 22 текстуры слоёв.
- JAR — **5 268 545 байт**. Проверены 11 обязательных классов/служебных файлов,
  исходная лицензия и побайтовое совпадение **1195 сгенерированных ресурсов JAR**.
  Нет legacy-классов, тестовых datapack или скопированных текстур Minecraft.
  Дерево legacy осталось `270b27b1cc52019733bb79facda68ce29aa038be`.

Linux CI завершён успешно 2026-09-12 в 13:52 Europe/Zurich:
[запуск 34692032555](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34692032555),
коммит `1b923dc426cc39d35645d71f0b01adfc39b9eed1`. Подтверждены 1229 сгенерированных
файлов, 117 Python-проверок, ресурсный граф, сборка/тесты ядра и все 658 GameTests:
645 основных, один обычного мира и 12 условных. `headSha` совпадает с исходным
коммитом, все шаги задания успешны; в игровых логах нет ERROR. Последующий коммит
фиксирует только документацию этого подтверждения.
Клиент на этом этапе не запускался. Приёмка внешнего вида, два реальных клиента,
остальные комплекты/интеграции ещё открыты. Следующий срез — ZombieSoldier,
его T1-экипировка и исходные правила Overworld-спавна.
Подробности: [T1_COMBAT.ru.md](T1_COMBAT.ru.md).


## ZombieSoldier и Overworld-пул — 2026-09-12

Итоговая Windows-проверка: **83 JUnit, 122 Python и 677 GameTests** —
664 основных (663 Techguns + один встроенный), один обычного мира и 12 условных.
Новый срез добавляет 19 GameTests, семь JUnit и пять Python.

- Проверены все четыре исходных оружия, реальные выстрелы и столкновения пуль,
  живые AI, смена автомата на лопату. Ближний урон железной/каменной лопаты —
  7,5/6,5 до защиты: базовые 4 плюс ванильные модификаторы предметов.
- Каждый бросок T1 независим, включая шлем; граница 0,5 включена.
  Яйцо вызывает оснащение, загрузка сохраняет оружие/патроны/износ/имя без переброса.
  Проверены собственная броня 5, toughness экипировки и отсутствие бонусов игрока.
- Нежить не получает poison/regen; отсутствует тег горения днём, лава наносит урон.
  Переведены пять живых JSON-пулов лута, разные прибавки «Добычи», фактическая смерть.
- Общий Overworld-пул: вес 600, группа 1–3, пороги 500/1000/2500, опасность биома,
  исходный порядок и включённая граница выбора. Все шесть исходных весов сохранены;
  пять неперенесённых записей пока дают пустой результат. Проверены вес 0,
  безопасный радиус, чёрный список, другие измерения, опора и освещение.
- Настоящий NaturalSpawner с исходными весами создаёт оснащённого солдата,
  после его смерти выпадает исходная ткань. В новом тестовом помещении
  выявлена гонка расчёта света: фиксированное ожидание виртуальных тиков
  заменено ограниченным ожиданием очереди освещения с обработкой задач сервера.
  Отдельный прогон всех 19 новых сценариев и итоговый общий прогон успешны.
- Оригинальная текстура переносится побайтово, подключены гуманоидный рендер,
  слои T1 и ванильные звуки. Клиент не запускался, визуальная приёмка открыта.
- Генератор: **1235 файлов**; граф: **172 item definitions, 292 ссылки на текстуры,
  57 звуков, 257 рецептов**. 168 реальных предметов, десять станков, четыре NPC.
- JAR: **5,324,646 байт**, проверены все **1199** генерируемых
  ресурсов внутри архива, новые классы NPC/селектора/рендера и исходная лицензия.
  Legacy, тестовые датапаки и копии ванильных текстур в современную сборку не входят.
  `legacy/1.12.2/src` остаётся деревом `270b27b1cc52019733bb79facda68ce29aa038be`.

Linux CI завершён успешно 2026-09-12 в 15:06 Europe/Zurich:
[запуск 34695337354](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34695337354),
коммит `f9c9d6e29a1ea4814eed835b665da81c5e5ca953`. Подтверждены 1235 сгенерированных
файлов, 122 Python-проверки, ресурсный граф, сборка/тесты ядра и все 677 GameTests:
664 основных, один обычного мира и 12 условных. `headSha` совпадает с исходным
коммитом; все шаги задания успешны, в полном журнале нет `/ERROR]`.
Этот чистый Linux-запуск также подтверждает ожидание освещения в новых чанках.
Последующий коммит меняет только документацию: фиксирует результат CI и
исходную зависимость следующего среза от T1 Miner.

Остаются остальные пять Overworld-записей, спавнеры/локации, другие измерения,
полная фракционная система, визуальная приёмка и остальная часть Techguns.
Следующий срез — ZombieFarmer/ZombieMiner. Подробности: `docs/ZOMBIE_SOLDIER.ru.md`.


## Клиентская проверка пользователя — 2026-09-12, 15:11–15:15

По прямой просьбе пользователя запущен клиент `71716f8` (код `f9c9d6e`):
Minecraft 26.2 / NeoForge 26.2.0.81, начало запуска 15:11:01, звук/атласы 15:11:32,
вход Dev в `New World` 15:12:46.619, штатный выход клиента 15:15:32.372 Europe/Zurich.
Пользователь сообщил, что всё понравилось, и попросил продолжить разработку.
Это общая положительная оценка сборки, а не отдельная приёмка каждой модели.
Есть нефатальная ошибка OSHI чтения счётчиков Windows Perflib; загрузке игры
она не помешала. Реестр Windows не изменялся. Computer Use и управление мышью/
клавиатурой не применялись. Журнал сохранён в `.tools/client-20260912-151101.runtime.log`.
Этот запуск предшествовал T1 Miner; его визуальная приёмка остаётся открытой.

## T1 Miner — 2026-09-12

Итоговая проверка на Windows завершена в 15:38 Europe/Zurich: **89 JUnit,
130 Python и 719 GameTests** — 706 основных (705 Techguns + один встроенный),
один обычного мира и 12 условных. Добавлены 42 GameTests, шесть JUnit и восемь Python.

- Четыре исходные части: по 825 прочности, физическая защита 13, остальные
  блокируемые категории 9,75, без toughness/радиации/отбрасывания. Проверены
  дробная защита, пробитие по частям, износ на 824 и разрушение смешанного комплекта.
- По 5% добычи и 8% движения на неизношенную часть; прыжок и падение у ботинок.
  Бонус добычи применяется к текущему BreakSpeed на NORMAL, учитывает эффекты,
  более ранние обработчики, состояние экипировки и отмену события. Предметы в
  инвентаре/второй руке и другие комплекты бонуса добычи не дают.
- Реальный ServerPlayerGameMode быстрее разрушает камень железной киркой с
  комплектом, сохраняя один износ инструмента. Тест явно инициализирует survival:
  настройки тестового сервера не должны незаметно превращать его в creative.
- Крафт: 6 слитков, 2 самородка, 15 тканей, один жёлтый краситель на комплект.
  Четыре цвета, особые имена цветов шлема, надевание, смена цвета, NBT/сеть,
  наковальня/книжные зачарования, ремонт/перекраска/Grinder проверены.
- Сохранены исходные выходы переработки, включая превращение новых ботинок
  из самородков в слитки. Особенность баланса отмечена в `docs/T1_MINER.ru.md`;
  её оценка остаётся в общей survival-приёмке.
- 1268 сгенерированных файлов; 176 item definitions, 296 ссылок на текстуры,
  57 звуков и 265 рецептов: 127 верстака, 19 печи, 119 машинных (46 Grinder).
  172 реальных предмета, четыре комплекта/16 частей, 15 equipment definitions,
  30 текстур слоёв. Исходные иконки и слои скопированы побайтово.
- JAR: **5,389,956 байт**; проверены все **1231** генерируемых
  ресурсов внутри архива, классы и исходная лицензия. Legacy, тестовые датапаки
  и копии ванильных текстур не включены; legacy-дерево по-прежнему
  `270b27b1cc52019733bb79facda68ce29aa038be`.

Linux CI завершён успешно 2026-09-12 в 15:54 Europe/Zurich:
[запуск 34697561640](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34697561640),
коммит `6f0904beeba26f747b402248fbd86e2d058023dc`. Подтверждены 1268 сгенерированных
файлов, 130 Python-проверок, ресурсный граф, сборка/тесты ядра и все 719 GameTests:
706 основных, один обычного мира и 12 условных. `headSha` совпадает с исходным
коммитом, все шаги задания успешны, в полном журнале нет `/ERROR]`.
Последующий коммит меняет только документацию и фиксирует это подтверждение.

Следующий срез — ZombieFarmer/ZombieMiner: броня для них готова, сами NPC ещё
не перенесены. Полная цель Techguns, остальные комплекты и визуальная приёмка остаются.

## ZombieFarmer и ZombieMiner — 2026-09-12

Итоговая проверка на Windows завершена в 18:25 Europe/Zurich: **95 JUnit,
136 Python и 745 GameTests** — 732 основных (731 Techguns + один встроенный),
один обычного мира и 12 условных. Добавлены 26 GameTests, шесть JUnit и шесть Python.

- Farmer: три мотыги/ручная пушка, 18 здоровья, 3 базового урона, обязательный
  нагрудник T1 Miner и отсутствие шлема. Miner: каменная/железная кирка/ручная
  пушка, 20 здоровья, 4 базового урона и обязательный шлем. Другие слоты имеют
  независимые исходные броски `<= 0.5`; одна из четырёх расцветок на всю экипировку.
- Проверены все оружия/цвета, пять видов ближнего оружия, семь живых AI,
  настоящий выстрел/попадание ручной пушки и бесконечные патроны NPC, исходная
  нулевая защита, нежить/эффекты/Smite, яйца и сохранение оружия/износа/имени/цвета.
- Дневной тест использует настоящий уровень освещения после завершения расчёта
  чанков: оба NPC горят 160 тиков на солнце, шлем не блокирует огонь и не изнашивается;
  ночь/непрозрачная крыша защищают, снятие крыши возвращает огонь.
  Для времени суток используется `MONSTERS_BURN` 26.2. Исключение исходного
  `TGSpawnerNPCData` ещё не перенесено вместе с самими Techguns-спавнерами.
- Семь пулов Farmer и пять Miner сохраняют исходные количества и прибавки
  «Добычи», включая количество ткани. Настоящая смерть даёт хлеб, которым
  питается игрок, либо железо/ткань, которыми Repair Bench чинит шлем T1 Miner.
- Настоящий NaturalSpawner с исходными весами 200/200 создаёт **обоих** новых NPC
  при опасности 0. Сохранено исходное `>= roll`: 201/199 исходов из 400.
  Существующий сценарий естественного спавна солдата проходит; три оставшихся
  неперенесённых записи по-прежнему дают пустой результат.
- Генератор: **1274 файла**. Граф: **178 item definitions, 296 ссылок на текстуры,
  57 звуков, 265 рецептов** (127 верстака, 19 печи, 119 машинных). Теперь 174 реальных
  предмета, шесть NPC и пять типов нежити. Все три обычных зомби используют
  исходный `zombie_soldier.png` через общий GenericZombieRenderer.
- JAR: **5 419 189 байт**; проверены **1235** генерируемых ресурсов архива,
  восемь записей классов/лицензии/mixin и отсутствие старого отдельного рендера.
  Legacy, тестовые датапаки и копии ванильных текстур не включены. Дерево legacy
  остаётся `270b27b1cc52019733bb79facda68ce29aa038be`; лицензия сохранена.

Linux CI завершён успешно 2026-09-12 в 18:45 Europe/Zurich:
[запуск 34705998005](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34705998005),
коммит `95651ae6ed16ea47095d233343bb59cff6f7333d`. Подтверждены 1274 сгенерированных файла,
136 Python-проверок, ресурсный граф, сборка/тесты ядра и все 745 GameTests:
732 основных, один обычного мира и 12 условных. `headSha` совпадает с исходным
коммитом, все шаги задания успешны, в полном журнале нет `/ERROR]`.
Последующий коммит меняет только документацию и фиксирует это подтверждение.

Следующий срез — T1 Scout, затем SkeletonSoldier: в исходнике для его обязательных
шлема и ботинок независимо выбирается Scout либо Combat. Визуальная приёмка
новых NPC/брони, остальные три Overworld-записи, спавнеры, интеграции и полный
перенос остаются открытыми. Подробности: `docs/RURAL_ZOMBIES.ru.md`.

## T1 Scout — 2026-09-12

Игровая проверка на Windows завершена в 19:02 Europe/Zurich: **100 JUnit,
143 Python и 783 GameTests** — 770 основных (769 Techguns + один встроенный),
один обычного мира и 12 условных. Добавлены 38 GameTests, пять JUnit и семь Python.

- Четыре бандитские части по 825 прочности, физическая защита 13 и остальные
  блокируемые типы 9,75. Проверены дробная защита/пробитие, нулевая toughness,
  износ на 824, сохранение защиты и разрушение при смешивании с обычной бронёй.
- По 12,5% движения на часть, прыжок 0,02 у одежды и 0,1 у ботинок. Настоящий
  `jumpFromGround` даёт 0,58 полного комплекта, 0,48 без бонуса ботинок и 0,42
  при полном износе. Проверены Speed II, Jump Boost I, смесь Scout/Combat/Miner,
  исключение инвентаря/второй руки и отсутствие у Scout бонуса добычи.
- Подсказки больше не округляют 12,5% до 13% и передают фактическую величину
  прыжка. Ботинки T2/Miner по-прежнему показывают 0,1; у Hazmat этот бонус отсутствует.
- Все четыре рецепта работают через RecipeManager: 5/8/7/4 плотной ткани,
  без металла. Проверены обычное надевание, четыре камуфляжа через предмет/станок,
  сохранение имени/износа/asset через NBT и сеть, наковальня и книжное зачарование.
- Repair Bench требует только ткань (2/4/3/2 на износе 824); недостаток отменяет
  всю операцию, постороннее железо не расходуется. Grinder выдаёт 3/5/4/3 ткани
  из новых вещей, 2/4/3/2 при износе 1 и одну ткань при 824 за 100 тиков/500 FE.
  Отдельно проходит цепочка ремонт → перекраска → переработка.
- Русские/английские названия вещей и четыре иконки/восемь слоёв взяты из оригинала.
  В исходных локализациях нет ключей камуфляжей Scout; добавлены описательные
  имена четырёх существующих текстур. Изменение отмечено в `docs/T1_SCOUT.ru.md`.
- Генератор: **1307 файлов**. Граф: **182 item definitions, 300 ссылок на текстуры,
  57 звуков и 273 рецепта**: 131 верстака, 19 печи, 123 машинных (50 Grinder).
  178 реальных предметов, пять комплектов/20 частей, 19 equipment definitions,
  38 исходных PNG слоёв. Количество NPC остаётся шесть: SkeletonSoldier ещё не перенесён.
- JAR: **5 483 575 байт**; проверены **1267** генерируемых ресурсов архива,
  шесть записей классов/лицензии/mixin и отсутствие legacy/тестовых датапаков/
  копий ванильных текстур. Дерево legacy по-прежнему
  `270b27b1cc52019733bb79facda68ce29aa038be`; исходная лицензия сохранена.

Linux CI завершён успешно 2026-09-12 в 19:10 Europe/Zurich:
[запуск 34707206680](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34707206680),
коммит `206d00c3afa16e6dcc8724734ead4b11538988ef`. Подтверждены 1307 сгенерированных файлов,
143 Python-проверки, ресурсный граф, сборка/тесты ядра и все 783 GameTests:
770 основных, один обычного мира и 12 условных. `headSha` совпадает с исходным
коммитом, все шаги задания успешны, в полном журнале нет `/ERROR]`.
Последующий коммит меняет только документацию: фиксирует CI и уточняет
назначение смещений оружия SkeletonSoldier по исходному рендеру.

Следующий срез — SkeletonSoldier: оба комплекта его экипировки готовы.
Визуальная приёмка Scout, остальные комплекты, специальные спавнеры, интеграции
и полный перенос остаются открытыми. Подробности: `docs/T1_SCOUT.ru.md`.

## SkeletonSoldier — 2026-09-13

Итоговая проверка на Windows завершена в 11:00 Europe/Zurich: **105 JUnit,
150 Python и 796 GameTests** — 783 основных (782 Techguns + один встроенный),
один обычного мира и 12 условных. Добавлены 13 GameTests, пять JUnit и семь Python.

- 25 здоровья, скорость 0,25, ближний урон 4, поиск цели 50; ширина/высота
  0,6/1,95, высота глаз 1,6575. Все четыре сочетания обязательных шлема/ботинок
  Scout/Combat сохраняют нулевую собственную броню. Combat добавляет по 0,5
  native toughness; девять урона пули полностью проходят, специального износа игрока нет.
- Все три достижимых оружия — револьвер, Thompson, ручная пушка — проверены
  настоящими попаданиями и живым AI. Нулевой сохранённый боезапас не мешает
  выстрелу NPC и не изменяется им. Самоцель/стена запрещают выстрел.
- Яйцо тратится в survival и создаёт вооружённого скелета. Все 12 сочетаний
  оружия/брони сохраняют пустые слоты, имя, износ, боезапас и изменённый камуфляж.
- Общий BurningUndeadNpc сохраняет солнечное поведение Farmer/Miner и скелета.
  Для Scout и Combat проверены 160 тиков огня без износа шлема, ночь, крыша и
  восстановление огня после снятия крыши. Специальная связь Techguns-спавнера
  остаётся неперенесённой; ванильная защита шлемом не подменяет исходное поведение.
- Четыре JSON-пула: уголь/порох/кость/ткань, исходные количества/«Добыча»;
  у угля повышается шанс, но не количество. Реальная смерть даёт две ткани,
  которыми Repair Bench чинит маску Scout на износе 824 с точным расходом.
- Общий NaturalSpawner с исходными весами создаёт **обоих** Overworld-солдат;
  опасность 0 исключает обоих. SkeletonSoldier занимает свои 100 исходов при
  опасности 1 и выше. PsychoSteve/Bandit сохраняют пустые исходы без перераспределения.
- Модель: четыре исходные тонкие конечности, UV/зеркальность/поворотные точки,
  расширение тела 0,0625 и слоёв брони 0,5/1,0. Смещения оружия −0,06/−0,06
  применяются в руке, X зеркалится слева. Ванильный PNG не копируется в мод.
  Рендер компилируется, визуальная приёмка остаётся.
- Генератор: **1311 файлов**. Граф: **183 item definitions, 300 ссылок на текстуры,
  57 звуков и 273 рецепта**: 131 верстака, 19 печи, 123 машинных (50 Grinder).
  179 реальных предметов, семь NPC, шесть типов нежити; броня остаётся в пяти
  комплектах/20 частях, с 19 equipment definitions и 38 исходными слоями.
- JAR: **5 507 147 байт**; проверены **1269** генерируемых ресурсов архива,
  восемь записей классов/лицензии/mixin и отсутствие legacy/тестовых датапаков/
  копий ванильных текстур. Дерево legacy остаётся
  `270b27b1cc52019733bb79facda68ce29aa038be`; лицензия и авторство сохранены.

Linux CI завершён успешно 2026-09-13 в 11:14 Europe/Zurich:
[запуск 34749055873](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34749055873),
коммит `2c49574a0045ba83ed937250babc0e168579be1e`. Подтверждены 1311 сгенерированных файлов,
150 Python-проверок, ресурсный граф, сборка/тесты ядра и все 796 GameTests:
783 основных, один обычного мира и 12 условных. `headSha` совпадает с исходным
коммитом, все шаги задания успешны, в полном журнале нет `/ERROR]`.
Последующий коммит меняет только документацию и фиксирует это подтверждение.

Следующий срез — Bandit: его оружие/Scout готовы. Для PsychoSteve необходимо
сначала перенести бензопилу. Визуальная приёмка, специальные спавнеры,
остальные системы и полный перенос остаются открытыми. См. `docs/SKELETON_SOLDIER.ru.md`.

## Bandit — 2026-09-13

Итоговая проверка на Windows завершена в 14:14 Europe/Zurich: **110 JUnit,
157 Python и 818 GameTests** — 805 основных (804 Techguns + один встроенный),
один обычного мира и 12 условных. Добавлены 22 GameTests, пять JUnit и семь Python.

- Шесть исходных оружий, включая достижимую болтовую винтовку в `default`,
  обязательные три части Scout и маска при `random <= 0.5`. Все 12 сочетаний
  сохраняют оружие, пустой боезапас, отсутствие маски, имя, износ и изменённый камуфляж.
- Живой моб: здоровье 20, скорость 0,3, атака 5, поиск цели 40, собственная
  броня 5. Яд/регенерация применяются, нет тегов нежити/инверсии лечения.
  Дневной свет с маской/без неё, ночь, крыша и её удаление не поджигают бандита;
  фактический урон лавы проходит. Используются исходные звуки жителя и шаги поверхности.
- Настоящие попадания и живой AI проверены для каждой пушки, включая восемь
  дробин обреза. Выстрел NPC не расходует сохранённый боезапас. Входящий урон
  подтверждён с маской и без: пуля AK-47 9 → 7,92, огонь 10 → 9, кислота 10 → 10.
  Бонусы/защита игрока и специальный износ Scout не переходят на NPC.
- Шесть пулов лута сохраняют metadata, количества и разные прибавки «Добычи».
  Ткань не получает прибавки количества. Проверены три реальные цепочки смерти
  → пистолетные/дробовые/винтовочные патроны → перезарядка револьвера/обреза/
  болтовой винтовки → выстрел игрока с точным расходом патронов и числом снарядов.
- Путь loot table нормализован в `entities/bandit`. Исходный PNG 64×64 скопирован
  побайтово. GenericNpcRenderer обобщён для Bandit и трёх зомби, сохраняя их текстуры.
  Рендер компилируется; визуальная приёмка остаётся.
- NaturalSpawner с прежними весами на повышенной опасности создаёт Bandit
  вместе с обоими солдатами. Отдельный натуральный сценарий опасности 1 допускает
  солдат и исключает Bandit; опасность 0 и отключение веса также проверены.
  Сохранены 49 из 653 исходов последней записи при стандартных весах и исходной
  границе `>= roll`. В обычном Overworld-пуле остаётся только неподключённый PsychoSteve.
- Генератор: **1315 файлов**. Граф: **184 item definitions, 300 ссылок на текстуры,
  57 звуков и 273 рецепта**: 131 верстака, 19 печи, 123 машинных (50 Grinder).
  180 реальных предметов, восемь NPC и прежние шесть типов нежити. Броня остаётся
  в пяти комплектах/20 частях, с 19 equipment definitions и 38 исходными слоями.
- JAR: **5 529 456 байт**; проверены **1272** генерируемых ресурса архива,
  шесть записей классов/лицензии/mixin и отсутствие старого GenericZombieRenderer.
  Legacy, тестовые датапаки и копии ванильных текстур не включены. Дерево legacy
  остаётся `270b27b1cc52019733bb79facda68ce29aa038be`; лицензия и авторство сохранены.

Linux CI завершён успешно 2026-09-13 в 14:29 Europe/Zurich:
[запуск 34757041834](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34757041834),
коммит `1fc5cb7665f1cc2762e61ba1daa6d56b5a9758fa`. Подтверждены 1315 сгенерированных файлов,
157 Python-проверок, ресурсный граф, сборка/тесты ядра и все 818 GameTests:
805 основных, один обычного мира и 12 условных. `headSha` совпадает с исходным
коммитом, все шаги задания успешны, в полном журнале нет `/ERROR]`.
Последующий коммит меняет только документацию и фиксирует это подтверждение.

Следующий срез — бензопила, затем PsychoSteve. Баки/заправка в Chemical Laboratory
уже доступны. Для бензопилы остаются расход/возврат бака, активация ПКМ,
ближний удар/добыча, отдельный снаряд и звуковой цикл.
Визуальная приёмка, прочие NPC/спавнеры/структуры, интеграции и полный перенос
остаются открытыми. См. `docs/BANDIT.ru.md`.

## Бензопила — 2026-09-14

Локальная Windows-проверка 15:30–15:31 Europe/Zurich: `build` и три headless
прогона завершились успешно. Всего **115 JUnit, 165 Python и 840 GameTests**:
827 основных (826 Techguns + один встроенный), один обычного мира и 12 условных.
Ошибок и пропусков нет; в итоговом игровом журнале нет `/ERROR]`.

Добавлены 20 специальных GameTests, общий цикл оружия и сценарий Grinder:

- Два реальных крафта пилы, последовательность лезвий 0 → 1 → 2 с остатками
  топлива 0/1/173, запрет повтора/понижения/пропуска; сохранение имени и удаление
  временных состояний. Сохранение ItemStack и синхронизация компонентов/рецепта.
- Лаборатория расходует 500 mB лавы и 100 FE, производит полный бак; из него
  проходит заправка на 45 тиков с возвратом пустого бака и реальная атака.
- Нативные удары игрока: 13 с топливом и 3 без; по броне 10 — 9,88 и 1,8.
  Последний расход немедленно снимает пробитие, а обновление атрибутов снимает
  бонус урона. Отвергнутый удар и creative не расходуют топливо.
- Нативный ServerPlayerGameMode разрушает бревно с последней единицей топлива
  и пустой пилой; в обоих случаях выходит один предмет бревна. Следующая рубка
  идёт со скоростью 1. Усиленные лезвия дают 14/17/20 и не заменяют кирку/лопату.
- PHYSICAL-атака наносит 7,6 по броне 10, учитывает hurt cooldown, перехватывается
  стеной, не разрушает её, проходит 6 блоков за два тика, игнорирует воду.
  Сохранение не продлевает жизнь атаки и сохраняет коэффициент сложности NPC.
- Спам, пустой/чужой стек и атака во время заправки отклоняются. Отмена создания
  снаряда другим обработчиком не расходует топливо.
- Grinder действительно перерабатывает пилу в пять железных слитков и пластик
  за 500 FE. Общие тесты подтверждают 51 рецепт Grinder.

Ресурсы: **1359 генерируемых файлов, 187 item definitions / 183 предмета,
308 ссылок на текстуры, 62 звуковых события, 280 рецептов**. 137 рецептов верстака,
19 печи и 124 машинных. Модели оружия: 645 деталей исходных конструкторов,
минус второй кадр лезвий плюс семь деталей заряженной ракеты = 651 деталь
статических поз 22 образцов. Повтор UV проверен на сохранение площади/направления
поверхности и границы атласа; копии PNG/OGG соответствуют исходным байтам.

JAR: **5 855 837 байт**, **1315** сгенерированных ресурсов архива сверены с
генератором. Проверены классы инструмента/атаки/кодека/свойства модели, исходная
лицензия, отсутствие legacy-кода, тестовых datapack и копий vanilla PNG в архиве.
Дерево `legacy/1.12.2/src` осталось `270b27b1cc52019733bb79facda68ce29aa038be`.

Локальные журналы: `.tools/chainsaw-game-verification.log`,
`.tools/chainsaw-python-full.log`, `.tools/chainsaw-audit-result.json`.
Первый Linux CI исходного среза acdf6e8 успешно завершился 2026-09-14 15:21 Europe/Zurich (запуск 34848259993).
После него добавлено Shift+ПКМ для обычного взаимодействия с блоками: обе проверки
нативного разрушения также открывают настоящий сундук и проверяют отсутствие расхода.
Повторные build и 20 GameTests пилы прошли 15:22–15:23; журнал .tools/chainsaw-input-verification.log.
Запуск 34848959681 на 8d74ffe выявил три прежних сбоя тестовой подготовки
дневного света: часы переключались, но skyDarken сохранял яркость предыдущего
тика. GameTestHelper.setTime не вызывает Level.updateSkyBrightness. Исправлен
только RuralDaylightGameTests: обновление после каждого переключения/восстановления
времени и явная предварительная ночная яркость, воспроизводящая прежнюю проблему.
Игровая логика NPC не менялась. Полный локальный прогон 15:30–15:31 вновь прошёл
все 840 GameTests; журнал перезаписан окончательным результатом. Сбойный CI-журнал
сохранён отдельно в .tools/chainsaw-ci-daylight-failure.log.
Linux CI окончательной исходной ревизии завершился **2026-09-14 15:36 Europe/Zurich**:
[запуск 34849857918](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34849857918), коммит `8f1c1aeaec1200c74a4912055e2cf91b2e43f8b4`.
Проверены точный headSha, успешный итог всех шагов, 1359 генерируемых файлов,
165 Python-проверок, ресурсный граф, сборка/тесты ядра и все 840 GameTests:
827 основных, один обычного мира и 12 условных. Ошибок `/ERROR]` в полном
журнале нет. Финальные журналы: `.tools/chainsaw-ci-result.json` и
`.tools/chainsaw-ci-full.log`. Следующий коммит меняет только этот протокол
и сводный статус; тестировалась именно указанная исходная ревизия.
Computer Use и интерактивный Minecraft не запускались. Анимация цепи, полный
управляемый звук, FX/GORE, точная приёмка sweep/зачарований и внешние уровни
инструментов остаются; это не завершение всего M4/M5 или полного Techguns.

## PsychoSteve — 2026-09-14

Локальная Windows-проверка 15:56–15:57 Europe/Zurich: `build` и все три headless
прогона прошли. **115 JUnit, 172 Python и 857 GameTests**: 844 основных
(843 Techguns + один встроенный), один обычного мира и 12 условных. Ошибок
и пропусков нет; в полном игровом журнале отсутствуют строки `/ERROR]`.

Добавлены 17 GameTests и семь Python-проверок:

- Живой NPC: 75 здоровья, скорость 0,6, follow range 60, базовая атака 7,
  собственная броня 5 и toughness 1. Заправленная пила даёт атрибут атаки 19;
  нативный AI использует PHYSICAL-атаку 10 с исходным коэффициентом сложности.
  Проверены подход с расстояния восемь блоков, попадание и граница AI в три
  блока; атаки себя и цели за стеной отклоняются. NPC не расходует топливо.
- Четыре общих камуфляжа полного T1 Miner, отсутствие игроковых бонусов/особого
  износа у NPC, яйцо и сохранение износа, имени, топлива и улучшенных лезвий.
  База опыта 25 сохраняется, нативная премия за пять предметов даёт 30–40.
- Фактический урон по PsychoSteve: AK-47 → 7,56, бензопила → 9,2, FIRE → 9,
  кислота → 10. Живые эффекты, отсутствие Smite-бонуса и дневного горения с
  шлемом и без проверены тем же подготовленным световым окружением.
- Два исходных пула лута: шансы 0,5 и прибавка 0,05 за уровень Looting;
  1–2 пилы без прироста количества и 2–4 бака с приростом 1–2 за уровень.
  Проверены Looting III, отдельные нестекаемые пилы и полный бак каждой.
  `set_components` выполняется до `set_count`, чтобы промежуточное количество
  два не нарушало проверку размера стека при установке компонентов.
- Все шесть исходных записей обычного Overworld-пула создают нужных NPC.
  Естественный спавн PsychoSteve при опасности 1 проверен с исходным весом 3
  и полным пулом 603, без повышения редкой вероятности. Опасность 0 исключает его.
- Нативный спавн → смерть → настоящие предметы → атака → заправка 45 тиков →
  повторная атака. Полная пила тратит единицу, один бак возвращает запас 300 и
  пустой корпус. Свежий лут не копирует модифицированное оружие погибшего NPC.
  Подключённый ServerPlayer в этой проверке имеет действительные Survival
  abilities; вызываются обе нативные фазы tick/doTick, включая cooldown и
  PlayerTickEvent.Post, а не только серверная фаза отправки данных.

Генератор: **1363 файла**; ресурсный граф: **188 item definitions / 184 предмета,
308 ссылок на текстуры, 62 звуковых события и 280 рецептов**. Оригинальная
128×128 army_soldier.png скопирована побайтно; сохраняется исходная UV-развёртка
гуманоидной модели 64×64. Теги нежити по-прежнему содержат шесть типов; PsychoSteve
не добавлен в эти теги. В этом срезе не добавлялись новые виды оружия или рецепты.

JAR: **5 884 864 байта**. Все **1318** сгенерированных ресурсов внутри архива
сверены с генератором. Проверены классы NPC/атаки/рендера и исходная лицензия;
legacy-код, тестовые datapack и vanilla PNG в архив не попали. Дерево
`legacy/1.12.2/src` осталось `270b27b1cc52019733bb79facda68ce29aa038be`.
Локальные журналы: `.tools/psycho-game-verification.log`,
`.tools/psycho-python-full.log`, `.tools/psycho-audit-result.json`.
Linux CI завершился **2026-09-14 16:08 Europe/Zurich**:
[запуск 34853141674](https://github.com/PozziTiv4ik/Techguns2/actions/runs/34853141674), коммит `dbf3d25e86a07dfe3dc4e18d33d29eb48ee2bce7`.
Проверены точный headSha, успешный итог всех шагов, 1363 генерируемых файла,
172 Python-проверки, ресурсный граф, сборка/тесты ядра и все 857 GameTests:
844 основных, один обычного мира и 12 условных. Ошибок `/ERROR]` в полном
журнале нет. Журналы: `.tools/psycho-ci-result.json`, `.tools/psycho-ci-full.log`.
Следующий коммит меняет только протокол и сводный статус; тестировалась именно
указанная исходная ревизия.

Computer Use и интерактивный Minecraft не использовались. Остаются визуальная
приёмка, анимация/звук/FX бензопилы, полные фракции, Techguns-спавнеры и локации.
Следующий срез — собственный NPC-спавнер и сохранение его связей с мобами.

## NPC-спавнер HOLE — 2026-09-20

Локальная Windows-проверка 14:19–14:20 Europe/Zurich: `build` и три headless
прогона прошли. **120 JUnit, 179 Python и 887 GameTests**: 874 основных
(873 Techguns + один встроенный), один обычного мира и 12 условных. Ошибок и
пропусков нет; в итоговом игровом журнале отсутствуют строки `/ERROR]`.

Добавлены 30 GameTests, пять JUnit и семь Python-проверок:

- Нативное размещение BlockItem выбирает исходный ZombieSoldier, 5 оставшихся
  смертей, максимум 3 живых NPC, задержку 200 и радиус 2. Проверены точная
  область выделения, отсутствие столкновения, Survival-добычи и дропа; кастомные
  BlockEntityData требуют оператора. Live UUID не отправляются через update tag.
- Спавн всех девяти перенесённых NPC вызывает finalizeSpawn с исходной
  экипировкой и назначает дом в радиусе 10. Проверены диапазоны координат,
  смещение высоты и отсутствие добавленных требований к освещению/игроку/опоре.
- Таймер, ограничение min(maxActive,mobsLeft), расход только после смерти,
  исчезновение без расхода, исчерпание и удаление блока. Отменённая смерть
  сохраняет место; повторные уведомления не расходуют счётчик второй раз.
- Отмена finalize, запрет spawn и отмена EntityJoinLevel не создают фантомных
  мест; после снятия обработчика новая попытка успешна. Peaceful и пустой пул
  сохраняют бюджет. ArmySoldier/vanilla zombie не заменяются доступным NPC.
- Override-оружие копирует компоненты и не разделяет изменяемый стек между
  вызывающим кодом, настройкой блока и двумя порождёнными NPC.
- Нативная причина удаления UNLOADED_TO_CHUNK сохраняет UUID-резервирование.
  NBT блока и NPC восстанавливаются в обоих порядках; relink идемпотентен,
  новый спавн не переполняет лимит, смерть восстановленного NPC учтена один раз.
- SavedData-кодек сохраняет объединённое уведомление. Настоящий callback смерти
  при незагруженном адресе владельца не загружает его чанк; после восстановления
  блока и очереди освобождает место и уменьшает бюджет ровно один раз.
- Замена владельца, включая копирование его NBT через размещаемый предмет,
  не принимает старые UUID. Межмерное удаление освобождает место; NBT-копия в
  другом измерении и её возврат не восстанавливают старую принадлежность.
- Метка происхождения предотвращает солнечное горение ZombieMiner и переживает
  сохранение даже при отсутствующем исходном блоке, как TGSpawnerNPCData.hasSpawner.
  Обычный NPC без метки горит. Невалидные сохранённые пределы нормализуются.
- Обычные веса спавнера имеют строгую границу, в отличие от Overworld-селектора.
  Проверены исходные 13 кубоидов, UV, PNG и локализованные названия без изменения
  старых undead-тегов, числа NPC, оружия или рецептов.

Генератор: **1370 файлов**; ресурсный граф: **189 item definitions / 185 предметов,
310 ссылок на текстуры, 62 звуковых события, 280 рецептов**. JAR: **5 925 969 байт**;
**1324** сгенерированных ресурса внутри архива сверены с генератором. Проверены
классы блока, link/mailbox и чистого ядра, исходная лицензия; legacy-код,
тестовые datapack и vanilla PNG в архив не попали. Дерево legacy осталось
`270b27b1cc52019733bb79facda68ce29aa038be`.

Локальные журналы: `.tools/spawner-game-verification.log`,
`.tools/spawner-python-full.log`, `.tools/spawner-audit-result.json`.
Linux CI завершился **2026-09-20 14:35 Europe/Zurich**:
[запуск 35510835091](https://github.com/PozziTiv4ik/Techguns2/actions/runs/35510835091), коммит `b53f4cf4b3d32c9b9a35ad306de4875aed789e66`.
Проверены точный headSha, успешный итог всех шагов, 1370 генерируемых файлов,
179 Python-проверок, ресурсный граф, сборка/тесты ядра и все 887 GameTests:
874 основных, один обычного мира и 12 условных. Ошибок `/ERROR]` в полном
журнале нет. Журналы: `.tools/spawner-ci-result.json`, `.tools/spawner-ci-full.log`.
Следующий коммит меняет только протокол и сводный статус; тестировалась именно
указанная исходная ревизия.

Это серверный срез HOLE и жизненного цикла девяти NPC. SOLDIER_SPAWN требует
ArmySoldier и берета T2. Остаются локации, остальные NPC, произвольный entity-NBT,
импорт 1.12.2, визуальная и длительная многопользовательская/аварийная приёмка.
Проверки сохранения используют реальные lifecycle callbacks, NBT/SavedData-кодеки
и незагруженный адрес владельца; отдельный restart/kill процесса не симулировался.
Computer Use и интерактивный Minecraft не использовались.


## ArmySoldier, берет T2 и SOLDIER_SPAWN — 2026-09-20

Локальная Windows-проверка 14:54–14:55 Europe/Zurich: `build` и три headless
прогона прошли. **122 JUnit, 188 Python и 918 GameTests**: 905 основных
(904 Techguns + один встроенный), один обычного мира и 12 условных. Ошибок и
пропусков нет; в итоговом игровом журнале отсутствуют строки `/ERROR]`.

Добавлены 31 GameTest, два JUnit и девять Python-проверок:

- ArmySoldier: 25 здоровья, скорость 0,30, базовый удар 4, follow range 75,
  собственная броня 8 и toughness 0. Живые эффекты/теги, голоса, базовый опыт,
  отсутствие бонусов скорости игрока на NPC; дневной свет с беретом и без него.
- Все 48 комбинаций трёх пушек и четырёх независимых роллов брони. Выбор шлема
  либо берета без пустой головы, очистка отсутствующих частей, исходный камуфляж
  0 и полные магазины. Проверен детерминированный распределённый набор 1024
  экипировок с достижимостью всех 16 подмножеств брони и всех трёх пушек.
- Настоящее яйцо, сохранение имени, пустого магазина, отсутствующих частей,
  износа и изменённого камуфляжа как шлема T2, так и берета. Реальные попадания
  и нативный ranged goal для M4, Combat Shotgun и Bolt Action.
- Урон AK-47 6,84 с беретом и 6,12 с полным T2: собственная броня 8 и нативная
  toughness экипировки без специальной защиты/износа игрока. Кислота наносит 10/10.
- Пять исходных пулов лута: ID, количества и разные шансы Looting у материалов
  и патронов; количество ткани без Looting-бонуса. Настоящая смерть вызывает
  собственную таблицу ArmySoldier.
- Берет: слот головы, прочность 825, три цвета, Shift+ПКМ, обычное надевание,
  сохранение и packet round-trip всех компонентов. Скорость 0,11 при ходьбе,
  0,156 при спринте; граница износа 823/824, восстановление после ремонта,
  отсутствие бонусов от предмета во второй руке.
- Физический, пулевой, огненный, ядовитый урон и падение игрока. Исходный
  четырёхтканевый крафт, наковальня, сохранение имени/цвета, отказ от металла,
  Repair Bench с двумя тканями в разных слотах, откат при нехватке и Camo Bench.
- Grinder: новый / повреждённый на 1 / изношенный до 824 берет даёт 3 / 2 / 1
  ткани за 500 FE. Сохранена исходная особенность округления здоровой брони.
- SOLDIER_SPAWN: настоящее размещение предметом, отличная от HOLE модель/блок,
  правильный ArmySoldier, общие исходные пределы/outline/коллизия/Survival,
  home, сохранение владельца, активный лимит, две смерти и удаление без дропа.
  Копирование custom pool сохраняет настройки и выдаёт новый UUID; старый NPC
  не расходует бюджет копии. В общий список проверок спавнера добавлен десятый NPC.
- Исходные PNG, UV, две детали ModelBeret с расширением 0,7/0,65 и наклоном −35°,
  привязкой к голове; модель/PNG военной точки, названия, крафт и теги. Берет
  остаётся единственной частью своей записи, без выдуманных ботинок/поножей.

В первом прогоне ресурсный аудит обнаружил предположение генератора, что
каждая запись брони имеет четыре части. Исправлено для крафта и Grinder:
они перебирают фактические предметы. В игровом прогоне три новых проверки
Grinder завершались раньше 100-тактовой операции из-за общего короткого лимита;
они перенесены в существующую группу `grinder_` с лимитом 400 тиков. Также
обновлены ожидаемые количества рецептов и предметов в старых проверках.
Итоговые полные прогоны после исправлений прошли.

Генератор: **1396 файлов**; ресурсный граф: **192 item definitions / 188 предметов,
313 ссылок на текстуры, 62 звуковых события, 282 рецепта** (138 верстака,
19 печи и 125 машинных, включая 52 Grinder). Пять полных комплектов брони плюс
берет: 21 предмет, 22 equipment definitions; число оружия осталось 22,
NPC стало десять, типов нежити осталось шесть. Естественный Overworld-пул
по-прежнему содержит шесть исходных вариантов, ArmySoldier в него не добавлен.

JAR: **5 974 774 байт**; **1346** сгенерированных ресурсов внутри архива
сверены с генератором. Проверены классы NPC/берета/спавнера и исходная лицензия.
Legacy-код, тестовые datapack, vanilla PNG и удалённые ошибочные рецепты
`t2_beret_helmet/chestplate/leggings/boots` в архив не попали. Дерево legacy:
`270b27b1cc52019733bb79facda68ce29aa038be` — без изменений.

Локальные журналы: `.tools/army-game-verification.log`, `.tools/army-python-full.log`,
`.tools/army-audit-result.json`; первые диагностические попытки сохранены отдельно.
Linux CI завершился **2026-09-20 15:06 Europe/Zurich**:
[запуск 35512338586](https://github.com/PozziTiv4ik/Techguns2/actions/runs/35512338586), коммит `bba03ef0e2e6460590c26b2a6096c2dd949ae074`.
Проверены точный headSha, успешный итог всех шагов, 1396 генерируемых файлов,
188 Python-проверок, ресурсный граф, сборка/тесты ядра и все 918 GameTests:
905 основных, один обычного мира и 12 условных. Ошибок `/ERROR]` в полном
журнале нет. Журналы: `.tools/army-ci-result.json`, `.tools/army-ci-full.log`.
Следующий коммит меняет только протокол и сводный статус; тестировалась именно
указанная исходная ревизия.

Computer Use и интерактивный Minecraft не использовались. Генерация геометрии и
client-классы проверены исходниками/сборкой; визуальная приёмка модели берета
(включая детский слой), NPC и военной точки остаётся открытой. Также остаются
длительный multiplayer/restart/аварийный тест, полные фракции, исходные локации,
остальные NPC и импорт старых миров. Следующий срез — локации и их спавнеры.


## Первая локация: малый адский алтарь и Nether Metal — 2026-09-20

Локальный итог 15:34–15:35 Europe/Zurich: `build` и три headless-прогона прошли.
**128 JUnit, 198 Python, 939 GameTests**: 925 основных (924 Techguns + один
встроенный), две настоящие проверки генерации и 12 условных. В последнем полном
игровом журнале нет `/ERROR]`; ошибок и пропусков тестов нет.

Добавлены шесть JUnit, десять Python и 21 GameTest:

- Десять исходных Nether Metal: hardness/resistance 8, требование кирки без
  выдуманного уровня, тот же variant в дропе, полная коллизия, только lava-рамка
  светится на 15. Оригинальный рецепт даёт 16 панелей из камня/незерака/железа.
- Camo Bench циклически меняет все десять вариантов в обоих направлениях,
  сохраняя стек из 32 предметов, имя, компоненты и правильный ITEM_MODEL.
- Все исходные PNG, два атласа, отдельный vanilla lava sprite без копирования
  Minecraft PNG, два слоя рамки и native light_emission 15. CTM-метаданные
  не выдаются за реализованную интеграцию. Исправлены предметные атласы HOLE
  и SOLDIER_SPAWN с сохранением их геометрии/пикселей.
- Бинарный template NBT: DataVersion 4903, размер 11×9×11, 769 уникальных
  исходных ячеек, palette и CyberDemon-спавнер в [5,7,5] без live UUID.
  Готовый NBT загружен штатным StructureTemplateManager; source hash нормализует
  CRLF/LF, gzip фиксирует timestamp и OS byte для повторяемой генерации.
- Четыре поворота: сверены все 769 ячеек, вырезанный воздух и направления лестниц.
  Chunk clipping: первая половина не ставит спавнер второй; сериализованный
  native piece восстанавливает origin, rotation и bounding box и завершает вторую.
- Фундамент: только заменяемые ячейки, один твёрдый слой не завершает колонну,
  два последовательных завершают; ограничение глубины 16 и сохранение твёрдых блоков.
- Настоящие NPC из размещённого шаблона: CyberDemon с Nether Blaster, параметры
  3 смерти / 2 живых / 200 тиков / радиус 1; третья смерть исчерпывает блок.
- Source grid с положительными/отрицательными координатами, приоритетом
  больших/средних точек и десятью билетами алтаря из 50/40. Пещера проверяет
  четыре угла, десять воздушных ячеек, диапазон (20,100] и разброс до 10;
  сохранены округление средней высоты, поворот вокруг центра и смещения источника.
- Codec структуры, native random-spread lattice 16/15, запрет Overworld даже
  при разрешающем biome predicate, флаг выключения Techguns-генерации.
- Реальная генерация Nether на seed 0: чанк **[16,416]**, origin **[256,42,6656]**,
  rotation NONE, спавнер **[261,49,6661]**. Загружены native start/piece и соседние
  части по bounding box; проверены реальный блок, параметры и native locate.
  Повторный запрос уже готового чанка сохраняет тот же start и UUID владельца.

Первый прогон естественной генерации выявил, что Minecraft GameTestServer
создаёт `WorldOptions(0, false, false)` и отключает структуры. Test-only mixin
включает их только при `techguns.worldgenTest=true`; основной GameTest-прогон
и обычные серверы сохраняют прежние опции. Отдельная Gradle-задача теперь
проверяет две функции `*_natural_chunks`, а CI запускает её с нормальным terrain
pack. Старый Python-контракт mining-tag обновлён с 19 до 29 после десяти блоков.

Генератор: **1482 файла**; ресурсный граф: **202 item definitions / 198 предметов,
339 ссылок на текстуры, 62 звуковых события, 283 рецепта** (139 верстака,
19 печи, 125 машинных). Арсенал 22, NPC десять, пять полных комплектов брони
и берет; их число в этом срезе не менялось.
JAR: **6 049 388 байт**, **1429** сгенерированных ресурсов сверены с архивом,
включая NBT и atlas definition. Исходная лицензия сохранена; legacy-классы,
тестовые datapack и vanilla PNG в JAR не попали. Legacy-tree остаётся
`270b27b1cc52019733bb79facda68ce29aa038be`.

Журналы: `.tools/altar-game-verification.log`, `.tools/altar-python-full.log`,
`.tools/altar-audit-result.json`. Первые диагностические попытки сохранены отдельно.
Linux CI завершился **2026-09-20 15:52 Europe/Zurich**:
[запуск 35514493434](https://github.com/PozziTiv4ik/Techguns2/actions/runs/35514493434), коммит `26a1b9ce1b6790eb826648fa54840cecaa4c7881`.
Проверены точный headSha, успешный итог всех шагов, 1482 генерируемых файла,
198 Python-проверок, ресурсный граф, сборка/тесты ядра и все 939 GameTests:
925 основных, две настоящие проверки генерации и 12 условных. Журнал содержит
подтверждение native Nether altar, ошибок `/ERROR]` нет.
Журналы: `.tools/altar-ci-result.json`, `.tools/altar-ci-full.log`.
Следующий коммит меняет только протокол и сводный статус; тестировалась именно
указанная исходная ревизия.

Computer Use и интерактивный клиент не использовались. Это первая локация,
не завершённый M10: остаются остальные структуры, несколько seed, проверки
сосуществования со сторонним worldgen, длительный multiplayer/restart/crash,
CTM/Chisel и визуальная приёмка. Native RNG/рельеф 26.2 не обещают координаты
мира 1.12.2. Следующий срез — остальные малые локации Nether, сундуки и их лут.


## NetherLoot01, сундук и охрана — 2026-09-20

Локальная сборка и все три headless-прогона завершены в 16:13 Europe/Zurich.
Итог: **130 JUnit, 204 Python, 956 GameTests** — 939 основных (938 Techguns
+ один встроенный), пять worldgen/selection и 12 условных. Ошибок и пропусков
нет; `/ERROR]` в полном игровом журнале нет. При подготовке обычного terrain
server один раз сообщил отставание на пять секунд; нагрузочная приёмка этим
прогоном не подтверждается. Существующие предупреждения RecipeManager о
recipe-book placement пользовательских машин остаются; рецепты проверены
через их настоящие машинные циклы.

Добавлены **14 основных + три worldgen GameTests, два JUnit и шесть Python**:

- Все 146 ячеек исходного scan 6×10×6; десять нижних колонн незерака,
  глубина 16, остановка после двух твёрдых блоков и сохранение одного
  промежуточного твёрдого слоя. Первоначальное предположение об отсутствии
  нижних ячеек отвергнуто полной проверкой scan до итоговой сборки.
- Четыре поворота вокруг [3,0,3], поворот сундука и сохранение исходного
  yaw=0 у обычного черепа скелета. Нативный RuleProcessor компенсирует
  поворот blockstate, который в 1.12 хранился отдельно в TileEntity.
- Chunk clipping и сохранение native piece восстанавливают шаблон,
  origin, rotation, границы с фундаментом и skull processor.
- Deferred LootTable в NBT: никакого заранее заполненного inventory/общего
  seed. Minecraft присваивает seed отдельно при размещении каждого сундука;
  исходный template NBT не изменяется.
- Полные 13 записей `chests/factory_building`: общий вес 97, 1–3 выбора,
  все границы весов и минимум/максимум количеств проверены реальной LootTable.
  Все шесть shared-metadata отображаются на уже работающие материалы.
- Открытие настоящего ChestMenu после сериализации неоткрытого сундука,
  сохранение открытого/опустошённого контейнера без повторной выдачи,
  извлечение настоящей воронкой с сохранением суммарного количества.
- Сундук → четыре плотные ткани → исходный крафт берета T2.
- Живой ZombiePigmanSoldier из шаблона: настройки 2/1/200/1,
  вооружение/принадлежность, две настоящие смерти → удаление спавнера;
  закрытый сундук не расходуется от боя.
- Общий начальный RNG чанка для двух native IDs сохраняет билеты и
  исключает одновременное появление алтаря и NetherLoot01. Проверены
  64 точки × три seed выбора × два состояния ore-cluster flag.
  Это проверка выбора, а не полная генерация трёх разных миров.
- Реальная генерация seed 0: положительная и отрицательная точки,
  native start/piece, фактический сундук/охранник, locate, отсутствие
  алтаря на этой точке и повторный запрос без замены сундука.

| Чанк | Основание шаблона | Поворот | Сундук |
|---|---|---|---|
| [16,528] | [256,31,8448] | COUNTERCLOCKWISE_90 | [258,33,8452] |
| [-16,-544] | [-256,31,-8704] | CLOCKWISE_90 | [-252,33,-8702] |

Прежний алтарь по-прежнему сгенерирован в [16,416], origin [256,42,6656].
Общий поиск пещеры и фундамент выделены в используемые обеими локациями
`SmallNetherStructure` / `NetherFoundation`; старые IDs и формат piece сохранены.

Генератор: **1488 файлов**. Ресурсный граф остался **202 item definitions,
198 предметов, 339 ссылок на текстуры, 62 звуковых события и 283 рецепта**.
JAR **6 077 533 байта**, **1434** сгенерированных ресурса побайтово сверены
с архивом, включая оба NBT и новую chest loot table. Исходная лицензия
сохранена; legacy-классы, тестовые datapack и vanilla PNG в JAR отсутствуют.
Legacy-tree: `270b27b1cc52019733bb79facda68ce29aa038be`, без изменений.

Журналы: `.tools/nether-loot-games-first.log`, `.tools/nether-loot-python-full.log`,
`.tools/nether-loot-audit-result.json`. Первые compile/Python-диагностики
исправлены до полного прогона.

Linux CI завершился **2026-09-20 16:24 Europe/Zurich**:
[запуск 35516101388](https://github.com/PozziTiv4ik/Techguns2/actions/runs/35516101388),
точная исходная ревизия `012ddaeb82b9b056512c7df9a2cb7d30b8057aca`. Все 13 шагов успешны;
проверены генератор (1488), Python (204), ресурсы, сборка/ядро и все
956 GameTests (939 + 5 + 12). Linux повторил обе точки NetherLoot01
и прежний алтарь на seed 0; `/ERROR]` в журнале нет.
Журналы: `.tools/nether-loot-ci-result.json`, `.tools/nether-loot-ci-full.log`.
Следующий коммит меняет только протокол/статус; тестировалась указанная
исходная ревизия.

Computer Use и интерактивный клиент не использовались. Визуальная приёмка,
перезапуск настоящего сервера, полные миры на нескольких seed,
сторонний worldgen, длительный multiplayer и импорт 1.12.2 остаются.
Следующий срез — NetherAcidHole. Подробности: `docs/NETHER_LOOT.ru.md`.


## NetherAcidHole и порядок размещения локаций — 2026-09-20

Локальная сборка и все три headless-прогона завершены в 16:42 Europe/Zurich.
Итог: **132 JUnit, 210 Python, 972 GameTests** — 953 основных
(952 Techguns + один встроенный), семь worldgen/selection и 12 условных.
Ошибок и пропусков тестов нет; `/ERROR]` в итоговом игровом журнале нет.

Добавлены **14 основных + два worldgen GameTests, два JUnit и шесть Python**:

- Исходный scan 9×6×9: 269 ячеек, 167 незерака, 60 воздуха, 21 песка душ,
  девять фиксированных источников и 12 случайных клеток на уровне 3.
  Все четыре поворота и все фиксированные/случайные ячейки проверены после
  нативного размещения; технических блоков/BlockEntity в готовой яме нет.
- 60 нижних колонн незерака: глубина 16, сохранение твёрдых блоков,
  один промежуточный слой допускает продолжение, два последовательных
  останавливают. Ячейки ниже предела не изменяются.
- Сохранена фактическая вероятность `MultiMBlock`: `nextInt(3)`, кислота
  при 0/1, незерак при 2, то есть 2/3 вместо кажущихся равными весов 1:1.
- Собственный `MixtureSeed` в native piece: значения 0, 1 и Long.MIN_VALUE
  переживают NBT; обратный порядок частей, промежуточная загрузка и другой
  population RNG дают поблочно тот же рисунок. Восемь seed дают разные края.
- Настоящая кислота из ямы: два poison-урона с hurt immunity, отсутствие
  отбрасывания/защиты обычной бронёй, сбор полного ведра, запланированное
  течение через пролом пола, отсутствие бесконечного восстановления источника.
- Три ведра из размещённой ямы переданы через обычное взаимодействие
  в реальную сформированную Reaction Chamber. На серверных тиках она
  расходует 100 mB/25 000 FE и создаёт две титановые руды плюс железную;
  2900 mB остаются в баке, пустые вёдра возвращены.
- Codec/измерение/настройка выключения, исходные билеты 30–39,
  общий выбор без наложения трёх локаций. Сохранены два неперенесённых
  исхода: Soul Platform и условный рудный кластер.
- Две реальные точки в basalt_deltas на seed 0: сохранённый start/piece,
  девять источников и весь случайный край, отсутствие другого TG-start
  на этой точке, native locate, seed смеси и повторный запрос готового чанка.

| Чанк | Основание | Поворот | MixtureSeed |
|---|---|---|---|
| [16,240] | [256,64,3839] | COUNTERCLOCKWISE_90 | -9217406516321840822 |
| [-16,-736] | [-257,58,-11776] | CLOCKWISE_90 | -3045455724851271275 |

Первый прогон обычного terrain выявил настоящую проблему: vanilla DELTA,
которая выполняется после native structures в `surface_structures`, заменила
девять источников кислотных ям лавой и магмой. Теперь все три малые локации
размещаются на `top_layer_modification`, после дельт/базальтовых колонн.
Повторный прогон на тех же координатах подтвердил сохранение кислоты.
Тест locate также исправлен: исходное смещение нечётного размера может
поместить origin в предыдущий чанк, поэтому поиск с радиусом 0 начинается
внутри самой ямы. Проверяется совпадение результата с чанком сохранённого start.

Полный повторный прогон после исправлений подтвердил прежний алтарь [16,416]
и NetherLoot01 [16,528] / [-16,-544], их NPC/сундуки и весь остальной порт.
Существующие миры не ретрогенерируются. Новый порядок — совместимость
с ванильными дельтами в этих проверках, не приёмка всех сторонних worldgen-модов.

Генератор: **1493 файла**. Ресурсный граф остался **202 item definitions,
198 предметов, 339 ссылок на текстуры, 62 звуковых события и 283 рецепта**.
JAR **6 100 463 байта**; **1438** сгенерированных ресурсов
побайтово сверены с архивом; проверен поздний этап всех трёх структур.
Исходная лицензия сохранена; legacy-классы, тестовые datapack и vanilla PNG
не включены. Legacy-tree неизменен: `270b27b1cc52019733bb79facda68ce29aa038be`.

Журналы: `.tools/nether-acid-game-verification.log`, `.tools/nether-acid-python-full.log`,
`.tools/nether-acid-audit-result.json`. Первые диагностические попытки сохранены
отдельно.

Linux CI завершился **2026-09-20 16:51 Europe/Zurich**:
[запуск 35517529886](https://github.com/PozziTiv4ik/Techguns2/actions/runs/35517529886),
точная исходная ревизия `2e239f4e72c6731e24e9d9cbda3b3ea2562a9fea`. Все 13 шагов успешны;
проверены генератор (1493), Python (210), ресурсы, сборка/ядро и все
972 GameTests (953 + 7 + 12). Linux повторил обе кислотные ямы в basalt_deltas,
включая MixtureSeed, две точки NetherLoot01 и прежний алтарь на seed 0.
`/ERROR]` в журнале нет. Журналы: `.tools/nether-acid-ci-result.json`,
`.tools/nether-acid-ci-full.log`. Следующий коммит меняет только
протокол/статус; тестировалась указанная исходная ревизия.

Computer Use и интерактивный клиент не использовались. Визуальная приёмка,
несколько seed полной генерации, настоящий restart/crash, multiplayer,
сторонний worldgen и импорт 1.12.2 остаются. Следующий срез — Ghastling
и NetherSoulPlatform. Подробности: `docs/NETHER_ACID.ru.md`.


## Ghastling и NetherSoulPlatform — 2026-09-20

Итог локальной сборки и трёх headless-прогонов: **138 JUnit, 218 Python,
1006 GameTests** — 985 основных (984 Techguns + один встроенный),
девять worldgen/selection и 12 условных. Последний полный прогон завершён
в 17:18 Europe/Zurich; `/ERROR]`, ошибок и пропусков тестов в нём нет.
Добавлены **32 основных + два worldgen GameTests, шесть JUnit и восемь Python**.

- Одиннадцатый NPC: наземный EntityMob, 20 здоровья, 1×2,1, глаза 1,5,
  follow range 64, базовые speed .7 / attack 2, vanilla fire immunity.
  Проверены гравитация, живые эффекты, исходные звуки/громкость и отсутствие
  выдуманного handheld gun/GenericNPC faction. Armor attribute=10 сохраняется,
  но фактическая типовая защита равна нулю, как возвращает исходник.
- Перенесён зарегистрированный AIFireballAttack, а не неиспользуемый
  AIGhastlingAttack: 30 тиков подготовки, выстрелы на 30/36/42,
  сброс на 48 и отдых 50; ближний бой каждые 20 тиков. Проверены
  граничные расстояния, сохранение cooldown после остановки goal,
  синхронизированное выражение лица и три настоящие сущности снарядов.
- Проверен живой GoalSelector с попаданием в цель. Исходный goal продолжает
  стрелять в уже выбранную цель за стеной; сама стена перехватывает снаряд.
- AlienBlasterProjectile профиля Ghastling: 6 FIRE, speed 1,5 с исходным
  множителем 1,5, spread .05, drag .99/.85, TTL 200, отсутствие gravity/
  block damage. Нет добавленного GenericNPC gun difficulty penalty;
  живой DamageFactorNPC действует при столкновении. Player Alien Blaster
  не добавлен в перечень перенесённых пушек; арсенал остаётся 22/44.
- Три секунды поджигания только после принятого урона, нулевой множитель,
  отмена hit/damage, witch resistance, обычная/типовая броня и fire-resistance
  effect. Владелец всегда исключается, даже при сохранённом LeftOwner=true.
  Проверены вода, стена без пожара, сохранение возраста и ровно 200 движений.
- Яйцо, native NBT и два исходных пула лута с Looting III. Зарядка является
  временным состоянием и после загрузки сбрасывается. Не добавлены обычная
  natural-spawn запись Ghastling, undead-теги или поведение летающего Ghast.
- Общая жизнь связанных со спавнером мобов вынесена в SpawnerNpc,
  отдельно от ArmedNpc/фракции. Имена прежних NBT-полей и SpawnerLink codec
  сохранены. Работают прежние десять NPC и новый Ghastling; проверена
  повторная загрузка с тем же UUID без дополнительной брони/пушки/слота.
- Модель: десять исходных кубоидов, Java Random seed 1660, UV 64×32,
  translation -0,6 в pivot и оригинальная синусоида щупалец; без vanilla
  Ghast scaling 4,5. Normal/attacking textures берутся из Minecraft.
  Рендер скомпилирован; визуальная приёмка, оригинальные FX/gore/Albedo остаются.
- Soul Platform: все 898 ячеек scan 13×10×13, включая 412 воздуха,
  24 skull и девять колонн фундамента. Source registration устанавливает
  size 11×11: сохранены pivot [5,0,5] и исходные смещения, а не центр 6.
  Проверены четыре поворота, каждый блок, сохранение yaw=0 у skull,
  clipping и повторная загрузка piece, глубина 16 / остановка на двух solids.
- Спавнер [6,6,6], Ghastling 3/2/200/1: настоящие появления и три смерти
  исчерпывают встречу. Четыре локации используют исходные отдельные билеты
  без наложения; неперенесённым остался условный рудный кластер.
- Две новые естественные точки на seed 0: native start/piece, сохранённый
  спавнер, 24 skull после генерации, locate и повторный запрос чанка.

| Чанк | Основание | Поворот | Спавнер |
|---|---|---|---|
| [16,480] | [255,43,7679] | CLOCKWISE_180 | [259,49,7683] |
| [-16,-144] | [-257,67,-2304] | CLOCKWISE_90 | [-253,73,-2298] |

Все прежние точки алтаря, NetherLoot01 и NetherAcidHole прошли регрессию.
Ранняя проверка общего спавнера ожидала оружие у каждого NPC; её уточнили
для исходно безоружного Ghastling. Старый тест rural_miner_live_ai_2 один раз
не подтвердил попадание за 75 тиков; два последующих полных прогона прошли
без изменения поведения Miner. Длительная стабильность AI остаётся частью
общей приёмки. Первые ошибки API имён PathType и типа частиц исправлены
до полного прогона. Диагностические журналы сохранены отдельно.

Генератор: **1503 файла**; **203 item definitions / 199 предметов,
339 ссылок на текстуры, 62 звуковых события, 283 рецепта**.
JAR **6 149 089 байт**, **1445** сгенерированных ресурсов
побайтово сверены с архивом. Исходная лицензия сохранена; legacy-классы,
тестовые datapack и vanilla PNG не включены.
Legacy-tree неизменен: `270b27b1cc52019733bb79facda68ce29aa038be`.

Журналы: `.tools/ghast-game-final.log`, `.tools/ghast-python-full.log`,
`.tools/ghast-audit-result.json`.

Linux CI завершился **2026-09-20 17:25 Europe/Zurich**:
[запуск 35519300940](https://github.com/PozziTiv4ik/Techguns2/actions/runs/35519300940),
точная исходная ревизия `145db8a6b8819e621af94c2ac22e3407640305e8`. Все 13 шагов успешны;
проверены генератор (1503), Python (218), ресурсы, сборка/ядро и все
1006 GameTests (985 + 9 + 12). Linux повторил обе Soul Platform,
обе кислотные ямы, две точки NetherLoot01 и прежний алтарь на seed 0.
`/ERROR]` в журнале нет. Журналы: `.tools/ghast-ci-result.json`,
`.tools/ghast-ci-full.log`. Следующий коммит меняет только
протокол/статус; тестировалась указанная исходная ревизия.

Computer Use и интерактивный клиент не использовались. Визуальная приёмка,
несколько seed полной генерации, настоящий restart/crash, multiplayer,
сторонний worldgen и импорт 1.12.2 остаются. Следующий срез — рудные
кластеры и NetherOreClusterSmall. Подробности: `docs/GHASTLING_SOUL_PLATFORM.ru.md`.


## Рудные кластеры и NetherOreClusterSmall — 2026-09-21

Полный локальный прогон: **139 JUnit, 225 Python, 1029 GameTests** —
1006 основных (1005 Techguns + один встроенный), 11 генерации/выбора и
12 условной химии. Ноль ошибок и пропусков. Добавлены 23 GameTests,
один JUnit и семь Python; сборка и серверные проверки прошли с первого прогона.

Перенесены девять блоков из BlockOreCluster и исходные TGConfig defaults.
GameTests проверяют фактическую установку каждого предмета, hardness −1,
нулевую скорость добычи незеритовой киркой, отсутствие дропа, неподвижность
для поршней, полный collision cube и отсутствие ticking/block entity.
Настройки подсказки меняются вживую; уровень/энергия/количество выводятся
в исходном порядке. Настоящий взрыв разрушает контрольную землю, сохраняя кластер.

Сопротивление **3 600 000** подтверждено по Minecraft 1.12.2 bytecode:
`Block.setResistance` (SRG func_149752_b) умножает аргумент 6 000 000 на 3,
`getExplosionResistance` (func_149638_a) делит поле на 5.
Оригинальные PNG и mcmeta скопированы побайтово в оба атласа. Переводы EN/RU
сохраняют прежние строки; при локальной генерации была замечена ошибочная
декодировка UTF-8, исправлена до коммита. Финальный JAR пересобран, все старые
ключи локализации сравнены с HEAD и не изменились.

NetherOreClusterSmall переносит все 27 ячеек 3×3×3, четыре поворота,
постоянный центр, пять смесей с незераком и одну с воздухом. JUnit перебирает
все 101 возможное значение: исходное включительное условие отдаёт кластеру
51 исход. Фундамент состоит из восьми магмовых колонн и смешанного центра,
глубина два блока. Тесты проверяют сохранение камня, отсутствие третьего слоя
и все восемь сочетаний независимых центральных ячеек на трёх высотах.
Сохранение piece между двумя клипами и другой population RNG сохраняет
полный шаблон и фундамент для seed 0, 1 и Long.MIN_VALUE.

Проверены native codec, оба флага генерации, запрет других измерений,
отсутствие наложения со всеми четырьмя остальными кандидатами и native locate.
Реальная генерация seed 0:

| Чанк | Origin | Поворот | MixtureSeed | Биом |
|---|---|---|---|---|
| [16,400] | [256,46,6400] | NONE | 7359095606865902688 | nether_wastes |
| [-16,-16] | [-256,59,-256] | NONE | -8091027484296320403 | basalt_deltas |

Магма, воздух, постоянный и случайные кластеры действительно присутствуют
после полной генерации чанков. Повторный запрос готового чанка ничего не
перебрасывает. Сохранён поздний этап после ванильных дельт.

Генератор: **1582 файла**; **212 item definitions / 208 предметов,
357 ссылок на текстуры, 62 звуковых события, 283 рецепта**.
JAR **6 339 859 байт**, **1521** сгенерированный ресурс побайтово проверен
внутри архива. LICENSE.txt сохранён, legacy и тестовые datapack не включены.
Дерево исходного legacy по-прежнему
`270b27b1cc52019733bb79facda68ce29aa038be`.

Проверки:

```text
powershell -NoProfile -File tools/dev.ps1 build runGameTestServer runOreWorldTestServer runChemistryTestServer --no-daemon --console=plain
python tools/generate_weapon_content.py --check
python tools/audit_legacy.py --check
python -m unittest discover -s tools/tests -v
python tools/verify_resources.py
```

Linux CI завершился **2026-09-21 18:27 Europe/Zurich**:
[запуск 35625191837](https://github.com/PozziTiv4ik/Techguns2/actions/runs/35625191837),
точная исходная ревизия `feba496d5fabeb9ae565f412d6d836baf19240ce`.
Все 13 шагов успешны: генератор (1582), Python (225), ресурсы, сборка/ядро,
все **1029 GameTests (1006 + 11 + 12)**. Linux повторил обе точки кластеров,
их координаты, биомы и сохранённые seed. Ошибок ресурсов в журнале нет.
Предупреждения об устаревающих версиях CI actions не помешали проверкам;
обновление этих зависимостей не входит в данный перенос блоков.

**Ore Drill и добыча из кластеров не реализованы.** Не добавлены выдуманные
дропы, истощение или восстановление. Следующий этап — многоблок бура,
головки, выход ресурсов, FE/топливо, баки и автоматизация. Остальные
кластерные структуры, интеграции, несколько seed и визуальная приёмка остаются.
Minecraft-клиент/Computer Use не запускались, релиз или бинарники не публиковались.
Подробнее: `docs/ORE_CLUSTERS.ru.md`.


## Ore Drill — 2026-09-21

Финальная локальная сборка и headless-прогоны: **147 JUnit, 1068 GameTests**
(1041 основной — 1040 Techguns + один встроенный; 12 worldgen/цепочка добычи;
15 условных интеграционных). Новых проверок: **39 GameTests, восемь JUnit
и семь Python**. В общем Python-прогоне 231 из 232 прошла, один старый тест
ожидал 29 блоков вместо 34 после добавления пяти частей бура. Ожидание
обновлено с проверкой каждого нового ID и отсутствия неразрушаемых кластеров;
все шесть проверок модуля руд прошли. Итоговый повторный прогон: **все 232 Python-теста прошли** за 236,7 секунды.

Перенесены пять частей бура, девять головок и 14 исходных рецептов.
Подключены все шесть осей, мини-бур 0/1 и четыре радиуса больших установок,
исходные длины/двигатели/каркас/решётки/воздушные зазоры и необязательная
концевая решётка. Минимальные конструкции содержат 2/19/102/273/556 блоков.
Неправильная конструкция не получает частичные связи. UUID защищает от
присваивания частей другой установки. Большой список частей кешируется,
а не создаётся заново каждый тик; после сборки и загрузки кеш сбрасывается.

Серверные проверки подтверждают:

- Каждую из шести осей мини-бура, четыре больших радиуса, правильные и
  неподходящие головки, постановку стержней от кластера, запрет второго
  доступа к связному кластеру и все предметы в настоящей творческой вкладке.
  Обнаруженный пропуск кластеров в этой вкладке предыдущего среза исправлен.
- Все границы исходных весов, включая 99/1 у угля и 50/40/10 у Nether Crystal.
  Это строгий WeightedRandom, а не включительный MultiMBlock структур.
- Полные циклы: уголь — 2057 тиков × 28 FE, адский кластер с малой
  обсидиановой головкой — 6000 × 48 FE. Результат один, кластер и головка
  сохраняются. При недостаточном уровне — булыжник, 400 × 24 FE.
- Связный размер ограничивается длиной бура; другой тип кластера прерывает
  обход. Сохраняются исходные Float-преобразования и округление длительности:
  например 0,2 у Shiny Gem даёт 119999 тиков в минимальном случае.
- Пауза без питания, возвращение FE, приоритет печного топлива, целое
  округление burn time, его минимум один за рабочий тик, переход остатка
  буфера на FE, ведро лавы и возврат обычного ведра, частичный бак лавы,
  значение зарегистрированного жидкого топлива, нулевая стоимость и
  общий machinesNeedNoPower. Отрицательный буфер при нулевой цене исправлен.
- Выход в девять слотов, исходный выброс переполненных предметов, исходная
  потеря лишней жидкости при полном выходном баке, сохранение заранее
  выбранного результата/прогресса/топлива и повторное подключение к стержню.
- Transaction rollback, только входное заполнение/выходное извлечение
  жидкостей, реальные вёдра и воронки, запрет автоматического извлечения
  головки/топлива, отказ сохранённых портов удалённого контроллера.
- 11 слотов GUI, размер головки, исходный запрет shift-click извлечения
  головки, 32-битные числа, полный FluidStack и защита от старого container ID,
  редстоун, владелец и отклонение кнопок за пределами радиуса 12.
- Отмена незавершённой операции при смене головки на исходном опросе раз
  в 20 тиков или разборке, без бесплатного результата/возврата потраченного FE.

Условный datapack проверяет источник нефти, три новых сценария бура,
непустые ore-теги, первый представитель и веса. Вода, лава и ванильные
предметы — тестовые представители; реальные сторонние моды не устанавливались.
Эти datapack в JAR не попадают.

В новом регионе настоящего Nether на seed 0:
контроллер **[254,47,68353]**, стержень **[255,47,68353]**, естественно
сгенерированный кластер **[256,47,68353]**. Полный цикл расходует 288 000 FE,
результат забирает нативная воронка, кластер остаётся. В первом прогоне выпала
кварцевая руда, в повторном — огненный стержень: выбор операции зависит от
мирового RNG, а не от фиксированного seed рисунка структуры.

Рендер: исходная логарифмическая форма, четыре секции на стержень,
чередование 45°, три PNG, 24 вершины/UV куба с исправленными нормалями,
полные габариты всей установки. Звуки: три события, 12 исходных OGG,
громкость 0,5, период 62 рабочих тика. GUI использует исходную текстуру.
CTM и визуальная приёмка остаются. Minecraft-клиент не запускался.

Генератор: **1699 файлов**; **229 item definitions / 222 предмета,
405 ссылок на текстуры, 65 звуковых событий, 297 рецептов**
(153 верстака + 19 печи + 125 машинных). Материалов/заготовок теперь 81.
JAR **7 175 479 байт**, **1636** сгенерированных ресурсов побайтово сверены
в архиве. Оригинальная лицензия сохранена, legacy и тестовые datapack исключены.
Дерево legacy неизменно: `270b27b1cc52019733bb79facda68ce29aa038be`.
В повторном Windows worldgen-прогоне ванильный LevelChunk один раз сообщил
о старом block entity в barrier внутри переиспользуемого GameTest-мира;
игровые проверки прошли, ошибок разбора ресурсов нет.

Команды:

```text
powershell -NoProfile -File tools/dev.ps1 build runGameTestServer runOreWorldTestServer runChemistryTestServer --no-daemon --console=plain
python tools/generate_weapon_content.py --check
python tools/audit_legacy.py --check
python -m unittest discover -s tools/tests -v
python tools/verify_resources.py
```

Первый Linux CI [35630785983](https://github.com/PozziTiv4ik/Techguns2/actions/runs/35630785983)
прошёл Python, сборку и 1041 основной GameTest, но упал на новой мировой
проверке воронки после ожидания 12 тиков. В тесте сохранялись ссылки на
block entities удалённого Nether-чанка без билета игрока. Теперь после
ожидания он загружает нужный чанк и заново получает живые экземпляры бура
и воронки. Сами правила добычи и передачи не ослаблены; при ошибке журнал
показывает, заменились ли экземпляры. Повторная локальная сборка и 12 + 15
мировых/условных проверок прошли 2026-09-21 19:22 Europe/Zurich.
Исправленная ревизия прошла Linux CI **2026-09-21 19:29 Europe/Zurich**:
[запуск 35631711097](https://github.com/PozziTiv4ik/Techguns2/actions/runs/35631711097),
точный source commit `307600ef5ce2cff98537f1b39e28a41633766176`.
Все 13 шагов успешны: 1699 сгенерированных файлов, 232 Python-теста,
ресурсный граф, сборка/ядро и все **1068 GameTests (1041 + 12 + 15)**.
В журнале `controllerReloaded=true, hopperReloaded=true`: на Linux чанк
действительно выгрузился между шагами. Получение живых экземпляров исправило
проверку; после загрузки полученная кварцевая руда успешно прошла в воронку.
Это подтверждает причину первоначального сбоя, а не просто повтор удачного
случайного прогона. Ошибок ресурсов в итоговом Linux-журнале нет.

Следующий этап — OreClusterSpike и другие кластерные структуры Верхнего мира.
Реальные сторонние руды/нефть/энергосети, JEI/CraftTweaker, CTM, визуальная
приёмка и длительный multiplayer остаются. Релиз/бинарники не публиковались.
Подробности: `docs/ORE_DRILL.ru.md`.


## OreClusterSpike, AlienBug и слизистые блоки — 2026-09-22

Windows: `build`, `runGameTestServer`, `runOreWorldTestServer` и
`runChemistryTestServer` завершились успешно в 11:56 Europe/Zurich.
Итого **153 JUnit, 240 Python, 1118 GameTests (1089 + 14 + 15)**.
Добавлены 50 GameTests: 11 для AlienBug, 37 для структуры/блоков
и две проверки естественной генерации. Новых JUnit — шесть, Python — восемь.

Проверены все 88 клеток scan для семи типов кластера и четырёх поворотов,
13 колонн очистки, сохранение между размещением частей, две независимые
встречи AlienBug по четыре смерти, оба флага и границы измерения.
У жука проверены характеристики, типовая защита, настоящая атака AI,
дневная цель-голем, яйцо, пустой лут, таймер укуса, фракция и спавнер.
Общий компонент SpawnerLifecycle сохранил прежний формат NBT и прошёл
существующие проверки смерти, отмены, выгрузки и смены измерения.
Проверяется также регистрация наездника, созданного на стадии finalizeSpawn.
Слизистые блоки проверяются по добыче, дропу, тегу лазания и запретам опор.

Первый серверный прогон нашёл удаление лестниц при разрешении смеси
камня/воздуха. Исправление возвращает исходный второй проход лестниц
и подавляет преждевременное обновление формы во время генерации.
Четыре проверки сохранения между частями теперь сохраняют точный рисунок,
в том числе две потенциально неподдерживаемые лестницы исходного scan.
В новой версии vanilla FREEZE_TOP_LAYER может добавить один слой снега
после структуры; только он допускается вместо воздуха в проверках
естественной генерации. Руды, лестницы и оба спавнера проверяются точно.

Две точки на seed 0:

- чанк **[-32,-4224]**, начало **[-512,66,-67584]**, поворот 180°,
  common_gem, mixtureSeed `6143134319047586874`;
- чанк **[32,6816]**, начало **[512,63,109056]**, поворот 180°,
  coal, mixtureSeed `4478242057429386903`.

Оба native start и `/locate` работают. Во второй точке настоящий Ore Drill
со стальной головкой выдал один предмет за 2057 рабочих тиков,
израсходовав 57 596 FE; исходный пул угольного кластера — угольная руда
с весом 99 и алмаз с весом 1. Природный кластер остался. Старые Nether-цепочки
и все 15 условных интеграционных проверок также проходят.

Генератор: **1750 файлов**, ресурсный граф: **232 item definitions /
225 предметов, 411 ссылок на текстуры, 71 звуковое событие, 297 рецептов**.
Скопированы исходная текстура AlienBug, 23 OGG, текстуры слизистых блоков
и анимация яиц. Модель жука — 17 частей / восемь связей, включая плоскость h3;
лестница — 22 кубоида. Инвентарная лестница сохраняет ориентацию metadata 3.
JAR — **8 026 178 байт**, **1684** сгенерированных ресурса побайтово
сверены в архиве; лицензия сохранена, legacy и тестовые datapack исключены.
Дерево legacy неизменно: `270b27b1cc52019733bb79facda68ce29aa038be`.
В окончательном Windows-журнале ошибок нет. Клиент не запускался.

Linux CI завершился успешно **2026-09-22 12:03 Europe/Zurich**:
[запуск 35713324916](https://github.com/PozziTiv4ik/Techguns2/actions/runs/35713324916),
точный source commit `4a0404f4ed35f4aad03da9dfb2daa34a6f8e788f`.
Все 13 шагов успешны: 240 Python, ресурсный граф, сборка/ядро и все
**1118 GameTests (1089 + 14 + 15)**. Обе естественные точки OreClusterSpike
и цикл добычи угля подтверждены на Linux; ошибок в итоговом журнале нет.

Следующий срез — OreClusterMeteorBasis. Проверка нескольких seed,
сосуществование со сторонними структурами, визуальная/звуковая приёмка
и длительная многопользовательская игра остаются.
Подробности: `docs/ORE_SPIKE_ALIENBUG.ru.md`.


## Commando и T2 Commando — 2026-09-22

Windows: `build` и три выделенных GameTest-запуска завершились успешно
в 15:20 Europe/Zurich. **159 JUnit, 1172 GameTests (1143 + 14 + 15)**;
новых GameTests — 54: 45 для брони/бонусов, восемь для NPC и один для
его создания через общий спавнер. Добавлены шесть JUnit и шесть Python.
Полный повторный прогон Python: **246 проверок**, 0 ошибок.

Commando проверен по здоровью/скорости/урону/дальности, собственным восьми
единицам брони, полной экипировке T2 Commando, Infiltrator, настоящему
снаряду и работе зарегистрированной AI-цели. Яйцо и сохранение сохраняют
имя, здоровье, четыре части брони, их износ и боезапас. Реальная смерть
использует его шесть исходных пулов; отдельно проверены вероятности
и минимальные/максимальные прибавки Looting. Общий конечный спавнер
создаёт именно Commando, сохраняя прежнюю обработку остальных NPC.
Проверка пропуска неперенесённого NPC теперь использует SuperMutantHeavy.

Четыре части брони по 990 прочности проверены через RecipeManager,
надевание, единственную текстуру, книги, наковальню, Repair Bench
и 12 вариантов переработки в Grinder: новая, слегка изношенная и
предельно изношенная часть для каждого слота. Подтверждены реальные
количества обсидиановой стали/резины и расход 500 FE на переработку.
Сырой каучук не проходит рецепт обработанной резины.

Проверены PHYSICAL, PROJECTILE, FIRE, EXPLOSION, POISON и RADIATION,
движение/спринт, прыжок/падение, сопротивление радиации и граница износа.
Скорость водной добычи проверена с настоящим погружением в WATER/LAVA,
а также в кислоте и молоке Techguns (исходный Material.WATER),
как глазами, так и только ногами; смешанный комплект с T1 Miner
перемножает обычный и водный бонусы. Точность проверяется непосредственно
в `GunItem.fire` у AK-47, дробовика, лазера, Nether Blaster, ракетницы
и бензопилы, включая прицеливание и дробины. Тест ракетницы считывает
исходный yaw из смещения дула: после выстрела её визуальный поворот
уже учитывает независимый гауссов шум скорости.

Шлем сохраняет кислородный признак и его отключение при износе.
Проверено, что без SCUBA-баллонов он не пополняет воздух. Баллоны
и дополнительные слоты остаются отдельной неперенесённой системой.

В первых прогонах исправлены ожидания тестов для уже надетого стека,
минимального float-ролла Looting, нового поддерживаемого NPC и номера
кнопки камуфляжного станка. В Python обновлена старая проверка объединения
тегов брони: теперь она требует 25 предметов, включая T2 Commando,
вместо прежних 21. Полный повторный прогон прошёл.

Ресурсы: **1778 сгенерированных файлов**, **237 item definitions /
230 предметов, 415 ссылок на текстуры, 71 звуковое событие, 305 рецептов**
(157 верстака + 19 печи + 129 машинных, включая 56 Grinder).
Исходная кожа ArmySoldier используется повторно; четыре иконки и два
слоя новой брони побайтово сохранены. JAR — **8 074 354 байт**,
**1710** сгенерированных ресурсов сверены внутри архива.
Лицензия сохранена; legacy и тестовые datapack не включены в JAR.
Дерево legacy неизменно: `270b27b1cc52019733bb79facda68ce29aa038be`.
В окончательном Windows-журнале ошибок нет. Клиент не запускался.

Ревизия `090368e74735560f1abf2cf224010f525f280194` прошла
[Linux CI 35732080132](https://github.com/PozziTiv4ik/Techguns2/actions/runs/35732080132)
с 1168 GameTests и 246 Python. Последующая сверка Material.WATER у
исходных жидкостей добавила поддержку кислоты/молока и ещё четыре
GameTests.

Окончательный Linux CI завершился успешно **2026-09-22 15:27 Europe/Zurich**:
[запуск 35732956071](https://github.com/PozziTiv4ik/Techguns2/actions/runs/35732956071),
точный source commit `b27e892dc974c6f4c8e672f943d84eb6ca09eb99`.
Все 13 шагов успешны: 246 Python, ресурсный граф, сборка/ядро и все
**1172 GameTests (1143 + 14 + 15)**. В итоговом журнале ошибок нет.
Проверены также четыре сценария для кислоты/молока; текущая ревизия
сохраняет полный исходный набор жидкостей Techguns для бонуса добычи.

OreClusterMeteorBasis пока не генерируется: NPC-зависимость готова,
остаются строительные блоки/лампы/дверь и сам шаблон из 2265 клеток.
Подробности и ограничения: `docs/COMMANDO.ru.md`.


## Панели, армированный бетон и металлические лестницы — 2026-09-22

Перенесены все 8 / 6 / 4 варианта исходных METAL_PANEL / CONCRETE / LADDER_0,
пять рецептов верстака и три палитры Camo Bench. Добавлены 63 GameTests
и восемь Python-проверок. Полный Windows-прогон `build runGameTestServer
runOreWorldTestServer runChemistryTestServer` завершился в 15:45 Europe/Zurich:
**159 JUnit, 1235 GameTests (1206 + 14 + 15)**; ошибок разбора данных нет.
Полный Python-прогон: **254 проверки**, без ошибок.

Проверены прочность/сопротивление, кирка начального уровня, звук и выпадение
всех восемнадцати блоков; 1/8 коллизия лестниц и отсутствие опорных граней;
четыре направления настоящей установки BlockItem, расход предмета,
сохранение без опоры, копирование направления снизу/сверху и нативный подъём.
Повороты/отражения и native BlockState codec сохраняют вариант и направление.
Camo Bench замыкает каждый исходный порядок в обе стороны, сохраняет стек
и имя, переключает модель и показывает отдельные названия цветов лестниц.
Шестнадцать сочетаний смешанного бетона, оба материала пластин и отрицательные
рецепты проверены через RecipeManager. Четыре обратных рецепта из неперенесённых
ступеней остаются явно отложенными.

Ресурсы: **1914 сгенерированных файлов**, **255 item definitions / 248 предметов,
451 ссылка на текстуры, 71 звуковое событие, 310 рецептов**
(162 верстака + 19 печи + 129 машинных). Сохранены исходные PNG и шесть кубоидов
лестницы; её инвентарная модель учитывает исходный NORTH-поворот на 180°.
JAR — **8 169 437 байт**; побайтово сверены **1844** сгенерированных ресурса,
наличие исходной лицензии и отсутствие legacy/test datapacks внутри архива.
Дерево legacy остаётся `270b27b1cc52019733bb79facda68ce29aa038be`.

Подробности: `docs/BUILDING_BLOCKS.ru.md`. CTM и визуальная приёмка остаются;
клиент и управление рабочим столом не использовались. Метеоритная база ещё
не генерируется: следующими нужны мешки с песком, лампы и бункерная дверь.


Linux CI подтверждён 2026-09-22 в 15:55 Europe/Zurich:
[запуск 35736021355](https://github.com/PozziTiv4ik/Techguns2/actions/runs/35736021355),
точный source commit `92de919205728f173649cca5f0bf0847a1181bb4`.
Все 13 шагов успешны: 254 Python, проверка ресурсов, сборка/ядро,
1206 основных GameTests, 14 генерации мира и 15 условных — всего **1235**.
В журнале нет ошибок. Последующий коммит обновляет только этот отчёт и статус.


## Мешки с песком, лампы и бункерная дверь — 2026-09-22

Добавлены шесть блоков/предметов и восемь исходных рецептов. Windows-прогон
`build runGameTestServer runOreWorldTestServer runChemistryTestServer`
завершился в 16:29 Europe/Zurich: **159 JUnit, 1327 GameTests (1298 + 14 + 15)**,
без ошибок. Полный Python-прогон: **264 проверки**, без ошибок.
Новый срез добавляет **92 GameTests и 10 Python-проверок**.

Мешки проверены во всех 16 сочетаниях сторон, в четырёх направлениях
диагонали, при обычном обновлении и флаге 2. Проверены исходная отдельная
коллизия углов, взаимосвязь с камнем/калитками/решётками, асимметрия заборов,
исключения, прочность 6, сопротивление 9, добыча рукой и факел сверху.
Два mixin восполняют старую callback-механику Forge и диагональные
render-состояния. Прежние тесты генерации и остальных блоков проходят.

Все четыре светильника проверены настоящей установкой предмета на шесть
граней, светом 15, коллизией, киркой, стеклянным звуком, выпадением одного
предмета, выбранной опорой, кронштейнами и сохранением направления.
Дополнительно проверены забор, воронка, наковальня, стена, мешки, стекло
и исходные горизонтальные исключения. Сохранение направления фонарей —
намеренное исправление потери ориентации у исходных metadata 12/13.

Дверь проверена по направлениям/петлям/обеим точкам ручного открытия,
совместному и независимому переключению, редстоуну на обеих высотах,
ручному действию под питанием, исходной логике выбора петли и звуку.
Реальный ServerPlayerGameMode добывает каждую половину: кирка даёт один
предмет, рука и creative — ноль. Потеря пола роняет одну дверь; чужая
верхняя половина не меняет тип нижней. Проверены поршневой запрет,
64 состояния модели и сериализация состояния блока.

Генератор/граф ресурсов: **1991 файл, 261 item definitions / 254 предмета,
498 ссылок на текстуры, 72 звуковых события, 318 рецептов**
(170 верстака + 19 печи + 129 машинных). Проверка blockstates теперь
обходит также multipart-модели и списки вариантов; ссылки не пропускаются.
PNG/OGG и собственная геометрия/UV сравнены с оригиналом. Старые ванильные
parent-модели двери заменены аналогами 26.2; торцевые UV наследуются от них.

JAR — **8 305 301 байт**; побайтово сверены **1920** сгенерированных ресурсов.
Архив содержит исходную лицензию, не содержит legacy и тестовые datapacks.
Дерево `legacy/1.12.2/src` остаётся `270b27b1cc52019733bb79facda68ce29aa038be`.
Клиент и управление рабочим столом не использовались. Визуальная приёмка,
проверка сторонних блоков и сама генерация метеоритной базы остаются.
Подробности и ссылки на первичные источники: `docs/FORTIFICATIONS.ru.md`.


Linux CI подтверждён 2026-09-22 в 16:37 Europe/Zurich:
[запуск 35740841069](https://github.com/PozziTiv4ik/Techguns2/actions/runs/35740841069),
точный source commit `fcf7ee0be7f118bfc0e02662abdaf880ff819373`.
Все 13 шагов успешны: 264 Python, проверка ресурсов, сборка/ядро,
1298 основных GameTests, 14 генерации мира и 15 условных — всего **1327**.
В журнале нет ошибок. Последующий коммит обновляет только этот отчёт и статус.


## OreClusterMeteorBasis — 2026-09-22

Проверены **161 JUnit**, **269 Python** и **1368 GameTests**
(1337 основных + 16 на новых чанках + 15 условных). Относительно
предыдущего среза добавлены два JUnit, пять Python и 41 GameTest.
Полный итоговый Windows-прогон завершился в 21:10 Europe/Zurich.
`build` и основной серверный набор прошли повторно после исправления
стыков чанков; природный и условный наборы также прошли. В журнале нет
ошибок загрузки данных или регистрации структур.

Пять Python-проверок сравнивают все 2265 ячеек исходного файла,
289 колонн расчистки, палитру, NBT, координаты и NBT пяти спавнеров,
включительные веса восьми типов и общий билет сетки средних локаций.
GameTests проверяют 32 сочетания типа/поворота, все клетки и смеси,
расчистку на 30 блоков, сохранение 64-битного зерна, обратный порядок
размещения частей, пять конечных встреч и конфигурационные запреты.
Две природные точки seed 0 дают кластер адского кристалла и урана;
структура находится через locate, сохраняется в Chunk, а бур
формируется напротив природного кластера.

На природных точках пять и 32 заглублённые клетки `minecraft:stone`
после размещения были заменены ванильной генерацией на угольную руду,
гравий и землю. В отдельном журнале подтверждено, что сразу после
`postProcess` на них был камень. Природный тест допускает до 64 таких
замен среди 1304 каменных ячеек, только ниже поверхности. Блоки базы,
смеси, спавнеры, дверь, лампы и соединения мешков проверяются строго.
При появлении соседнего чанка модельный край мешков и стеклянных панелей обновляется
в полосе один блок; обе природные структуры прошли этот сценарий.
Это отличие нативного порядка генерации Minecraft 26.2 от полной
поблочной копии 1.12.2; детали: `docs/METEOR_BASE.ru.md`.

Генератор формирует **1996 файлов**, из них **1924** ресурса побайтово
сверены в JAR размером **8 333 705 байт**. Архив содержит
лицензию и не содержит `legacy/1.12.2` либо тестовые datapacks.
Дерево исходников остаётся `270b27b1cc52019733bb79facda68ce29aa038be`.
Minecraft-клиент и управление рабочим столом не запускались.
Визуальная приёмка, другие seed и сторонние структуры ещё требуют проверки.

Linux CI подтверждён 2026-09-22 в 21:32 Europe/Zurich:
[запуск 35773704595](https://github.com/PozziTiv4ik/Techguns2/actions/runs/35773704595),
точный source commit `65437fa74bd5d73da48f658a8ec5d17ec97df8a7`.
Все 13 шагов успешны: 269 Python, сборка/ядро, 1337 основных GameTests,
16 природных и 15 условных — всего **1368 GameTests**. Предыдущий
случайный сбой природного угольного кластера устранён: тест теперь принимает
оба исходных результата добычи, включая алмаз с весом 1. Последующий
коммит обновляет только этот отчёт и статус.


## AlienBugNest — 2026-09-26

Полный Windows-прогон `build runGameTestServer runOreWorldTestServer
runChemistryTestServer` завершился успешно в 11:34 Europe/Zurich:
**166 JUnit, 1381 GameTest (1348 + 18 + 15)**. Полный Python-прогон:
**273 проверки**, без ошибок. Добавлены пять JUnit, четыре Python
и 13 GameTests.

Независимый Python-тест компилирует исходный `AlienBugNest.java` и исходные
геометрические методы `BlockUtils` с минимальным миром записей блоков.
Все клетки 64 раскладок совпали с портом: 16 seed, положительные/отрицательные
координаты, сплошная порода/плоская поверхность. Это проверка алгоритма
расстановки, без имитации Minecraft-физики и обновлений соседей.

GameTests проверили шесть раскладок, повторную сериализацию между каждым
чанком при обратном порядке размещения, исходные параметры спавнеров,
семь действительных смертей AlienBug при лимите три живых, добычу и
неподвижность закалённого песка, реестры, конфиги и две природные точки:

- Чанк **[32,4160]**: 10 вершин графа, 5274 клетки,
  границы `[488,44,66539]..[547,73,66609]`,
  layoutSeed `-8909463777696872438`, decorSeed `6046417856762815255`.
- Чанк **[-32,-6976]**: 8 вершин графа, 4703 клетки,
  границы `[-514,49,-111625]..[-457,78,-111581]`,
  layoutSeed `8477405307440499149`, decorSeed `4114611443367973432`.

Все сохранённые клетки обеих природных структур совпали с миром.
Native locate и сохранение работают. Гнездо продолжает генерироваться
при выключенных рудных структурах; большие узлы и другие измерения исключены.

Генератор: **2008 файлов**. В JAR размером **8 371 205 байт** побайтово
проверены **1935 ресурсов**, присутствует исходная лицензия; legacy-код и
тестовые datapacks в архив не попали. Дерево `legacy/1.12.2/src` осталось
`270b27b1cc52019733bb79facda68ce29aa038be`. Управление рабочим столом и
Minecraft-клиент не использовались.
Ограничения и следующие проверки: `docs/ALIENBUG_NEST.ru.md`.

Linux CI завершился успешно 2026-09-26 в 11:46 Europe/Zurich:
[запуск #52 / 36233475237](https://github.com/PozziTiv4ik/Techguns2/actions/runs/36233475237),
точный source commit `e4688bcd509a98b060348f0a05d85197d38b8209`.
Все 13 шагов успешны: 273 Python, сборка/ядро, 1348 основных GameTests,
18 природных и 15 условных — всего **1381**. Обе природные точки,
числа комнат, клеток и сохранённые seeds совпадают с Windows.
Последующий коммит обновляет только этот отчёт и статус.


## NetherOreClusterCastle — 2026-09-26

Windows: полный прогон `build runGameTestServer runOreWorldTestServer
runChemistryTestServer` завершился в 12:01 Europe/Zurich без ошибок:
**169 JUnit и 1394 GameTests (1359 + 20 + 15)**. Добавлены три JUnit,
пять Python-проверок исходных ресурсов и 13 GameTests.

Проверены все 642 клетки, палитра из девяти элементов, семь гарантированных
и 13 случайных клеток кластера. Четыре поворота и четыре обратных
размещения по частям сохраняют смесь, NBT охраны, фундамент и соединения
адского забора. Фундамент ограничен 16 исходными нижними клетками;
проверены глубина 16, пропуск твёрдых блоков и остановка после двух подряд.

Все четыре спавнера создают именно ZombiePigmanSoldier с болтовой винтовкой.
Проверены стрельба этой винтовкой, две действительные смерти на каждом
посту, лимит одного живого NPC и исчезновение всех четырёх постов.

Две природные крепости на seed 0:

- Чанк **[32,4128]**, начало **[511,55,66048]**, поворот по часовой стрелке 90°,
  mixtureSeed `-1308434924461066819`.
- Чанк **[-32,-4128]**, начало **[-512,31,-66049]**, поворот против часовой
  стрелки 90°, mixtureSeed `4194547523865807853`.

Все клетки обеих структур прошли строгую сверку с шаблоном/смесью.
Native locate, отсутствие малых локаций на том же среднем узле и
сохранение идентичности четырёх спавнеров при повторной загрузке проверены.
На положительной точке настоящий Ore Drill выполнил 6000 рабочих тиков,
потратил **288 000 FE**, выдал один ресурс из исходного адского пула
и сохранил бесконечный кластер. Выключение рудных структур, общий запрет,
узлы большой сетки и другие измерения проверены отдельно.

Генератор: **2013 файлов**; в JAR размером **8 397 329 байт** побайтово
сверены **1939 ресурсов**. Лицензия присутствует, legacy-кода и тестовых
паков в архиве нет. Дерево `legacy/1.12.2/src` неизменно:
`270b27b1cc52019733bb79facda68ce29aa038be`.
Полный Python-прогон: **278 проверок**, без ошибок.
Управление рабочим столом и Minecraft-клиент не использовались.
Визуальная приёмка и другие природные seed остаются;
подробности: `docs/NETHER_CLUSTER_CASTLE.ru.md`.

Linux CI завершился успешно 2026-09-26 в 12:12 Europe/Zurich:
[запуск #53 / 36234803969](https://github.com/PozziTiv4ik/Techguns2/actions/runs/36234803969),
точный source commit `fba11e3c04cce8f028f76569bb8c08d0da4a34d2`.
Все 13 шагов успешны: 278 Python, сборка/ядро, 1359 основных GameTests,
20 природных и 15 условных — всего **1394**. Координаты, повороты и
mixtureSeed обеих природных крепостей совпали с Windows; полный цикл
добычи из положительной точки также прошёл.
Последующий коммит обновляет только этот отчёт и статус.

## NetherAltarMedium — 2026-09-26

Windows: полный прогон `build runGameTestServer runOreWorldTestServer
runChemistryTestServer` завершился в 12:58 Europe/Zurich без ошибок:
**169 JUnit и 1409 GameTests (1372 + 22 + 15)**. Полный Python-прогон:
**284 проверки**, без ошибок. В этом срезе добавлены шесть Python-проверок
и 15 GameTests; число JUnit не изменилось.

Перенесены все **1668 клеток** исходного алтаря, включая 932 клетки воздуха,
15 записей палитры и четыре HOLE-спавнера. Проверены четыре поворота
вокруг `[8,0,8]`, обратный порядок настоящих чанков с сериализацией после
каждого размещения, 16 колонн фундамента, глубина 16 и остановка после
двух твёрдых блоков подряд. Четыре поста создают CyberDemon с Nether Blaster:
лимит один живой, 200 тиков, радиус один, три смерти на пост.
Проверены **12 действительных смертей** и исчезновение всех четырёх постов.

Алтарь и рудная крепость используют общий поиск пещеры и первый RNG-выбор
из исходного пула 10/10/1000. Выключение рудных структур оставляет алтарь
в пуле 10/10; общий запрет, другое измерение и узлы большой сетки исключают
обоих кандидатов. Native codec, locate и отсутствие наложения кандидатов
проверены. NetherGhastSpawner пока остаётся пропущенным билетом.

Два природных алтаря на seed 0:

- Чанк **[32,8864]**, начало **[512,66,141824]**, поворот `NONE`, четыре поста.
- Чанк **[-32,-14304]**, начало **[-512,29,-228864]**, поворот `NONE`, четыре поста.

Все клетки обеих структур прошли строгую сверку, включая воздух и формы
оград/ступеней; UUID спавнеров сохраняются. Обе прежние природные крепости
сохранили координаты, повороты и mixtureSeed из предыдущего раздела;
цикл настоящего бура на положительной крепости также проходит.

Природная проверка обнаружила два вмешательства соседних ванильных чанков:
`DeltaFeature` заменял металлическую панель лавой, а `BasaltColumnsFeature`
заполнял 12 заданных шаблоном клеток воздуха базальтом. Их исходные списки
защищённых блоков расширены тегом из **20 собственных блоков**:
десять Nether Metal, девять рудных кластеров и NPC-спавнер. GameTests
вызывают настоящие генераторы, проверяют все 20 блоков, обе ветви поиска
базальтовых колонн и положительные контроли лавы/колонн на обычном рельефе.

Ограды и ступени поставлены в штатную очередь обработки форм. Природный
тест загружает соседнее кольцо FULL-чанков и выполняет
`LevelChunk.postProcessGeneration`, как `ChunkMap.prepareTickingChunk`
при активации чанка сервером. Проверка после этой фазы сравнивает мир
с шаблоном, не восстанавливая содержимое вручную.

Устранены также две зависимости тестов от окружения: NPC новой встречи
наблюдаются через `EntityJoinLevelEvent` с освобождением обработчика после
теста, а восемь вариантов смеси кластера проверяются в постоянной
изолированной позиции с ограниченным перебором seed. Проверки реальных
смертей, привязки NPC и всех восьми вариантов сохранены.

Генератор: **2019 файлов**; в JAR размером **8 426 543 байта** побайтово
сверены **1944 ресурса**, а также конфигурация mixin. Ресурсный граф:
262 item definitions, 500 ссылок на текстуры, 72 звуковых события,
318 рецептов. Лицензия присутствует; legacy-кода и тестовых datapacks
в архиве нет. Дерево `legacy/1.12.2/src` неизменно:
`270b27b1cc52019733bb79facda68ce29aa038be`.
Управление рабочим столом и Minecraft-клиент не использовались.

Визуальная приёмка, несколько природных seed и взаимодействие
со сторонними структурами ещё остаются;
подробности: `docs/NETHER_MEDIUM_ALTAR.ru.md`.

Linux CI завершился успешно 2026-09-26 в 13:10 Europe/Zurich:
[запуск #54 / 36237687993](https://github.com/PozziTiv4ik/Techguns2/actions/runs/36237687993),
точный source commit `45cf8dd8ce71c01494e50fbc3191ace1b6a19313`.
Все 13 шагов успешны: 284 Python, сборка/ядро, 1372 основных GameTests,
22 природных и 15 условных — всего **1409**. Координаты, повороты и четыре
поста обоих природных алтарей совпали с Windows. Обе крепости сохранили
свои mixtureSeed; добыча буром также прошла. Следующий коммит обновляет
только этот отчёт и статус.


## NetherGhastSpawner — 2026-09-26

Windows: полный прогон `build runGameTestServer runOreWorldTestServer
runChemistryTestServer` завершился в 19:28 Europe/Zurich без ошибок:
**169 JUnit и 1425 GameTests (1385 + 25 + 15)**. Полный Python-прогон:
**290 проверок**, без ошибок. Добавлены шесть Python-проверок и
16 GameTests: 13 основных и три природных/выбора; JUnit не изменились.

Перенесены все **738 клеток**, включая 473 клетки воздуха, 13 исходных
записей палитры, три напольных черепа и 18 железных прутьев. Проверены
четыре поворота вокруг `[5,0,5]`, обратное размещение настоящих чанков
с сериализацией после каждого, форма прутьев и 13 колонн фундамента.
Фундамент сохраняет глубину 16, пропуск твёрдых клеток и остановку
после двух подряд. Черепа сохраняют исходный yaw 0.

Шаблон создаёт обычный `minecraft:mob_spawner`, `SpawnData` содержит
только `techguns:ghastling`. Проверены штатные параметры 20/200/800,
четыре попытки, лимит шесть поблизости, радиусы игрока 16 и появления 4.
Игрок за пределами радиуса не продвигает таймер; рядом создаются реальные
Ghastling, достигают лимита шесть и блокируют дальнейшие циклы.
После шести действительных смертей спавн возобновляется, спавнер остаётся.
Конечная квота и связь с HOLE не добавлены; вид и таймер сохраняются.
Проверка выполняется в Незере: открытое небо Overworld отклоняется
унаследованным `Monster/PathfinderMob.checkSpawnRules` из-за освещения.
Правила появления не подменяются тестовыми разрешениями.

Исходный сундук смотрит на юг и использует таблицу factory_building.
Проверены отложенный seed, настоящее открытие меню, точное содержимое
по исходной таблице, сохранение открытого и опустошённого сундука без
повторной выдачи. Общая таблица имеет 13 исходных ресурсов.

Две природные клетки на seed 0:

- Чанк **[32,35264]**, начало **[512,32,564224]**, поворот по часовой 90°;
  сундук **[520,36,564231]**, спавнер **[517,37,564228]**.
- Чанк **[-32,-28640]**, начало **[-512,33,-458240]**, поворот `NONE`;
  сундук **[-505,37,-458238]**, спавнер **[-508,38,-458235]**.

Все 738 клеток каждой точки строго совпали с шаблоном после штатной
финальной обработки чанков; locate, сохранение сундука и отсутствие
других локаций на том же узле проверены. Оба прежних средних алтаря и
обе крепости сохранили координаты и повороты; mixtureSeed крепостей
не изменились, реальный цикл бура также проходит.

Отдельный тест перебрал выбор на seed 0/1/42, в положительных и
отрицательных координатах, с рудными структурами и без них. Каждый
кандидат потребляет свои исходные билеты; одновременно выбирается
максимум одна локация. Теперь все три кандидата пула 10/10/1000
реализованы. Без рудных структур оба кандидата пула 10/10 появляются.
Глобальный запрет, другие измерения и узлы большой сетки исключены.

Генератор: **2024 файла**; в JAR размером **8 454 383 байта** побайтово
проверены **1948 ресурсов** и конфигурация mixin. Ресурсный граф:
262 item definitions, 500 ссылок на текстуры, 72 звуковых события,
318 рецептов. Лицензия присутствует; legacy-кода и тестовых datapacks
в архиве нет. Дерево `legacy/1.12.2/src` неизменно:
`270b27b1cc52019733bb79facda68ce29aa038be`.
Управление рабочим столом и Minecraft-клиент не использовались.

Визуальная приёмка, другие природные seed и сторонняя генерация ещё остаются;
подробности: `docs/NETHER_GHAST_SPAWNER.ru.md`.

Linux CI завершился успешно 2026-09-26 в 19:38 Europe/Zurich:
[запуск #55 / 36259323648](https://github.com/PozziTiv4ik/Techguns2/actions/runs/36259323648),
точный source commit `698ef43360c6cfc07dca23ba2de563ebb14c5ff9`.
Все 13 шагов успешны: 290 Python, сборка/ядро, 1385 основных GameTests,
25 природных/выбора и 15 условных — всего **1425**. Обе природные клетки,
координаты сундуков/спавнеров и повороты совпали с Windows. Оба средних
алтаря и обе крепости также сохранили результат, включая добычу буром.
Последующий коммит обновляет только этот отчёт и статус.


## ZombiePoliceman и PoliceStation — 2026-09-26

Windows: полный прогон `build runGameTestServer runOreWorldTestServer
runChemistryTestServer` завершился в 20:13 Europe/Zurich без ошибок:
**172 JUnit и 1454 GameTests (1411 + 28 + 15)**. Python: **298 проверок**,
выполнены пакетами 99/99/100; манифесты сверены с discovery, пропусков,
дублей, ошибок и skipped нет. Добавлены три JUnit, восемь Python и
29 GameTests: 12 для NPC, 14 для участка, три природных/выбора.

Проверены исходные параметры четырнадцатого NPC, все 32 сочетания оружия
и брони, включительная граница шанса 0,5, полицейский камуфляж 5,
настоящее появление из яйца, сохранение экипировки/здоровья/боезапаса,
два профиля настоящих снарядов и два обычных ranged goal. Дневной тест
проверяет восемь секунд горения, отсутствие защиты шлемом, ночь, крышу
и освобождение от дневного горения для NPC из спавнера. Пять исходных
пулов добычи проверены с Looting и через действительную смерть.
Восемь прежних Python-проверок объединённых тегов обновлены для седьмой
записи нежити; прежние NPC и исключения живых NPC сохранены.

Участок содержит 1212 исходных клеток. Все четыре поворота и обратное
размещение настоящих чанков с сохранением проходят строгую сверку,
включая направления/петли двух дверей, рельсы, верхние плиты,
лестницу, шесть ламп и соединения прутьев/стёкол/мешков.
Фундамент правильно использует исходный `CONCRETE_BROWN` metadata 0:
169 колонн, три заменяемых блока вниз, без адского правила остановки
после двух твёрдых. Очистка семи блоков вверх охватывает также клетки,
которых нет в scan; восьмой слой сохраняется.

Три SOLDIER_SPAWN-поста создают ZombiePoliceman с исходными параметрами
3/1/200/1. Проверены девять реальных смертей и удаление каждого поста
после его третьей смерти. Мобы сохраняют связь со своим постом.

Три исходных блока сундуков сохраняют собственные отложенные loot seeds.
Две северные половины соединяются в настоящий контейнер на 54 слота.
Проверены все 32 записи четырёх пулов и заряженное оружие: начальная
нулевая metadata исходного `GenericGun` даёт полный боезапас, поэтому
в современной таблице явно записан `techguns:rounds`. Открытие сундука,
выстрел из награды, сохранение состава инвентаря и опустошение без
повторной выдачи прошли игровые сценарии.

Две природные точки на seed 0:

- Чанк **[32,4192]**, начало **[511,68,67071]**, поворот 180°,
  три поста и три блока сундуков.
- Чанк **[-32,-4288]**, начало **[-512,64,-68609]**, поворот против часовой 90°,
  три поста и три блока сундуков.

После завершения соседних чанков все клетки/очистка прошли строгую
сверку. Native locate, неизменность сундуков при повторном запросе чанка
и отсутствие других средних локаций на том же узле проверены.
Выбор сравнивается с MeteorBasis, OreClusterSpike и AlienBugNest на
seed 0/1/42, положительных и отрицательных координатах, с рудными
структурами и без них. Участок занимает ровно десять исходных билетов;
глобальный запрет, другие измерения и большая сетка исключены.

Первый общий прогон выявил нестабильное попадание в прежнем сценарии
фермера с ручной пушкой за 75 тиков. Для вооружённых вариантов этого
AI-теста цель приближена с пяти до трёх блоков; проверка настоящего AI,
снарядов и уменьшения здоровья сохранена, дистанция подхода с инструментом
не изменена. Последующий полный прогон прошёл; механика оружия не менялась.

Генератор: **2034 файла**. В окончательном JAR размером **8 514 625 байт**
побайтово проверены **1956 ресурсов**. Ресурсный граф: 263 item definitions,
500 ссылок на текстуры, 72 звуковых события, 318 рецептов.
Исходная лицензия присутствует; legacy-кода и тестовых datapacks в архиве нет.
Дерево `legacy/1.12.2/src` осталось
`270b27b1cc52019733bb79facda68ce29aa038be`.
Управление рабочим столом и интерактивный Minecraft-клиент не использовались.

Визуальная приёмка, другие природные seed и сторонняя генерация остаются;
подробности: `docs/POLICE_STATION.ru.md`.

Linux CI завершился успешно 2026-09-26 в 20:25 Europe/Zurich:
[запуск #56 / 36262011805](https://github.com/PozziTiv4ik/Techguns2/actions/runs/36262011805),
точный source commit `f01f814061ff56c6aeeb617491354f651a156a7a`.
Все 13 шагов успешны: полный единый запуск 298 Python за 209,491 секунды,
сборка/ядро, 1411 основных GameTests, 28 природных/выбора и 15 условных —
всего **1454**. Оба участка совпали с Windows по координатам, поворотам,
трём постам и трём блокам сундуков. Прежние системы, включая тест фермера
с ручной пушкой, прошли общий Linux-прогон. Последующий коммит обновляет
только этот отчёт и статус.


## Камуфляжные сети и навесы — 2026-09-26

Перенесены шесть блоков двух семейств, 16 горизонтальных соединений каждого,
исходные коллизии/контуры выбора, свободная установка, добыча и 30 моделей.
Три исходных PNG побайтово сохранены в двух атласах; `submodel` старых Forge
blockstates преобразован в одновременные части multipart. Восстановлено
одностороннее примыкание стеклянных панелей и железных прутьев к вертикальной
сетке через узкий mixin. Заборы и навесы этого соединения не получают.

Камуфляжный станок меняет лесную/пустынную/снежную окраску в обе стороны,
сохраняя полный стек и его компоненты. Два исходных рецепта дают 8/16 блоков.
Отдельный тег земли сохраняет metadata 0 из Forge OreDictionary; современные
виды почвы не становятся неявной заменой.

Новые проверки:

- 113 GameTests: 96 сочетаний семьи/окраски/соединений, настоящая установка
  BlockItem, потеря опоры, hand drops, прочность и звук, соседние панели/заборы,
  повороты/отражения, сохранение, RecipeManager, оба цикла Camo Bench и финальная
  обработка соседних форм после шаблона с `UPDATE_KNOWN_SHAPE`.
- Девять Python: исходный enum и metadata, все одновременные submodels,
  таблица навесов, geometry/UV/display, PNG, рецепты, тег земли, лут и переводы.

Полная локальная проверка: 172 JUnit, 307 Python без ошибок/пропусков и
1567 GameTests (1524 основных + 28 природных/выбора + 15 условных).
Python выполнялся тремя пакетами 102/102/103; их объединение сверено с полным
unittest discovery без повторений. Команда `build runGameTestServer
runOreWorldTestServer runChemistryTestServer` завершилась с кодом 1 на старте
последнего сервера, без диагностического сообщения, после двух успешных
GameTest-наборов. Отдельная `build runChemistryTestServer` прошла с кодом 0
и `BUILD SUCCESSFUL` в 20:52 Europe/Zurich. Причина прекращения первого процесса
не установлена; его начало условных тестов не засчитано как успешная проверка.

Локальный JAR: 8 580 337 байт. Все 2013 сгенерированных ресурсных записей
внутри архива побайтово совпадают с генератором; LICENSE сохранён, legacy-код
и тестовые worldgen/chemistry-паки не попали в сборку. Генератор — 2093 файла,
ресурсная проверка — 269 item definitions, 557 texture references,
72 sound events, 320 рецептов. Дерево исходника 1.12.2 осталось
`270b27b1cc52019733bb79facda68ce29aa038be`.

Журналы: `.tools/camonets-targeted.log`, `.tools/camonets-full.log`,
`.tools/camonets-chemistry.log`, `.tools/camonet-python-batch-{0,1,2}.log`;
сводка сверки архива, JUnit и полного Python discovery — `.tools/camonet-audit.json`.

Клиентская визуальная приёмка и интеграция Chisel остаются. SurvivorHideout
пока не включён: дальше нужны зажигательные магазины, смена боеприпасов и
стрельба, затем перенос исходной структуры с полной добычей. Отдельные
зажигательные патроны уже существуют как продукция лаборатории/сырьё Grinder.
См. `docs/CAMOUFLAGE_NETS.ru.md`. Рабочий стол и интерактивный клиент не использовались.

Linux CI завершился успешно 2026-09-26 в 21:02 Europe/Zurich:
[запуск #57 / 36264230110](https://github.com/PozziTiv4ik/Techguns2/actions/runs/36264230110),
точный source commit `b570fca304dbf7ae3dc0f0e98be3b357db947824`.
Все 13 шагов успешны: 307 Python единым запуском за 211,549 секунды,
сборка/ядро, 1524 основных GameTests, 28 природных/выбора и 15 условных —
всего **1567**. Ресурсный граф совпал с Windows: 2093 generated files,
269 item definitions, 557 texture references, 72 sound events и 320 рецептов.
Два контрольных участка PoliceStation совпали по координатам, поворотам,
трём постам и трём блокам сундуков. Все серверы завершились штатно отдельными
Gradle-командами. Полный журнал: `.tools/camonets-ci-full.log`.
Следующий коммит изменяет только документацию: фиксирует этот результат CI.
