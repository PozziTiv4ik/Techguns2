# Techguns — Minecraft 26.2 development port

Рабочий форк [оригинального Techguns2](https://github.com/pWn3d1337/Techguns2).
**Полный перенос ещё не завершён. Это исходники разрабатываемого порта, не готовый релиз.**

Основная платформа: **Minecraft Java 26.2 · NeoForge 26.2.0.81 · Java 25**.
Другие версии Minecraft и загрузчики пока не поддерживаются.

- [Подробный план полного переноса](docs/PORTING_PLAN.ru.md)
- [Текущий статус и ограничения](docs/STATUS.ru.md)
- [Каталог 761 исходного Java-файла и 44 записей оружия](content/legacy-inventory.json)
- [Оригинал 1.12.2](legacy/1.12.2/README.md)

## Сборка

Установить JDK 25 (например, Temurin), задать `JAVA_HOME` на его директорию.
Gradle Wrapper скачает закреплённые зависимости при первом запуске.

```powershell
.\gradlew.bat build
.\gradlew.bat :neoforge-26.2:runGameTestServer
.\gradlew.bat :neoforge-26.2:runClient
```

Linux/macOS: использовать `./gradlew` с теми же аргументами.
Первый запуск требует интернета и времени на подготовку Minecraft.
Локальная сборка появляется в
`platforms/neoforge-26.2/build/libs/techguns-26.2-neoforge-0.1.0-dev.jar`.

На этом рабочем месте JDK также находится в игнорируемой Git папке `.tools/jdk25`.
Она не является частью репозитория: после клонирования установить собственный JDK 25.
Для использования локального JDK без настройки окружения в этом проекте:
`powershell -File tools/dev.ps1 build` (вместо `build` можно указать задачу запуска).

## Что можно попробовать

В новой тестовой игре с командами:

```mcfunction
/give @s techguns:revolver
/give @s techguns:pistolrounds 16
```

Предметы также доступны во вкладке Techguns. Новый револьвер пустой.
Удерживать ПКМ 45 тиков (2,25 секунды), чтобы зарядить пустой барабан.
ПКМ заряженным оружием — выстрел. Для ранней перезарядки удерживать Shift + ПКМ.
Отпускание ПКМ или смена предмета отменяет перезарядку.
Один предмет патронов заряжает шесть выстрелов — это правило исходного револьвера.
Полоса предмета показывает боезапас.

Сейчас управление и визуальные эффекты предварительные: исходные клавиши,
HUD, анимации, варианты патронов и остальные системы перечислены в статусе.
Survival-рецепты пока не включены. Использовать новые тестовые миры;
импорт сохранений 1.12.2 не реализован.

## Структура

| Директория | Назначение |
|---|---|
| `core` | Правила оружия без зависимости от Minecraft; unit tests |
| `platforms/neoforge-26.2` | Современный мод, предметы, пуля, ресурсы и GameTests |
| `legacy/1.12.2` | Сохранённые исходники и ресурсы оригинала; не компилируются новой сборкой |
| `content` | Воспроизводимый каталог исходников |
| `tools` | Проверка каталога и преобразование ресурсов |
| `docs` | План, статус и инструкции |

Для проверки каталога и исходных ресурсов нужен Python 3.10+ без сторонних библиотек:

```text
python tools/audit_legacy.py --check
python tools/generate_revolver_resources.py --check
```

## Авторство и распространение

Оригинальные код, модели, текстуры и звуки принадлежат авторам Techguns.
Сохраняется [Techguns Mod License](LICENSE.txt). Форк предназначен для разработки
и возможного вклада в исходный проект. Выпуск изменённого мода требует разрешения
авторов. CI проверяет исходники и тесты; релизы и бинарные сборки не публикует.
